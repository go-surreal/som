// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package repo

import (
	"context"
	"errors"
	"fmt"
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	"slices"
	som "som.test/gen/som"
	conv "som.test/gen/som/conv"
	index "som.test/gen/som/index"
	internal "som.test/gen/som/internal"
	query "som.test/gen/som/query"
	relate "som.test/gen/som/relate"
	with "som.test/gen/som/with"
	model "som.test/model"
)

type DrawingRepo interface {
	// Query returns a new query builder for the Drawing model.
	Query() query.Builder[model.Drawing]
	// Create creates a new record for the Drawing model.
	Create(ctx context.Context, drawing *model.Drawing) error
	// Insert creates multiple records in a single operation.
	// Before- and after-create hooks are invoked for each node.
	Insert(ctx context.Context, nodes []*model.Drawing) error
	// CreateWithID creates a new record with the given ID for the Drawing model.
	CreateWithID(ctx context.Context, id string, drawing *model.Drawing) error
	// Read returns the record for the given ID, if it exists.
	Read(ctx context.Context, id string) (*model.Drawing, bool, error)
	// Update updates the record for the given Drawing model.
	Update(ctx context.Context, drawing *model.Drawing) error
	// Delete deletes the record for the given Drawing model.
	Delete(ctx context.Context, drawing *model.Drawing) error
	// Refresh refreshes the given model with the current database state.
	Refresh(ctx context.Context, drawing *model.Drawing) error
	// Relate returns a new relate builder for the Drawing model.
	Relate() *relate.Drawing
	// Resolve loads the given relations of the model from the database.
	Resolve(ctx context.Context, drawing *model.Drawing, fetch ...with.Fetch_[model.Drawing]) error
	// Index returns a new index instance for the Drawing model.
	Index() *index.Drawing

	// OnBeforeCreate registers a hook that runs before a record is created.
	// If the hook returns an error, the create operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeCreate(fn func(ctx context.Context, node *model.Drawing) error) func()
	// OnAfterCreate registers a hook that runs after a record has been created.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterCreate(fn func(ctx context.Context, node *model.Drawing) error) func()
	// OnBeforeUpdate registers a hook that runs before a record is updated.
	// If the hook returns an error, the update operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeUpdate(fn func(ctx context.Context, node *model.Drawing) error) func()
	// OnAfterUpdate registers a hook that runs after a record has been updated.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterUpdate(fn func(ctx context.Context, node *model.Drawing) error) func()
	// OnBeforeDelete registers a hook that runs before a record is deleted.
	// If the hook returns an error, the delete operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeDelete(fn func(ctx context.Context, node *model.Drawing) error) func()
	// OnAfterDelete registers a hook that runs after a record has been deleted.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterDelete(fn func(ctx context.Context, node *model.Drawing) error) func()
}

// drawingRepoInfo holds the model-specific conversion functions for Drawing.
var drawingRepoInfo = RepoInfo[model.Drawing]{
	CreateNew: func(ctx context.Context, db *dbConn, target string, data any) (*model.Drawing, error) {
		raw, err := dbCreateNew[conv.Drawing](ctx, db, target, data)
		if err != nil {
			return nil, err
		}
		return conv.ToDrawingPtr(raw), nil
	},
	CreateOne: func(ctx context.Context, db *dbConn, id models.RecordID, data any) (*model.Drawing, error) {
		raw, err := dbCreate[conv.Drawing](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToDrawingPtr(raw), nil
	},
	InsertAll: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) ([]*model.Drawing, error) {
		raw, err := dbInsert[conv.Drawing](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		results := make([]*model.Drawing, len(raw))
		for i, r := range raw {
			results[i] = conv.ToDrawingPtr(r)
		}
		return results, nil
	},
	MarshalOne: func(node *model.Drawing) any {
		return conv.FromDrawingPtr(node)
	},
	MergeOne: func(node *model.Drawing, data []byte) error {
		into := conv.FromDrawingPtr(node)
		if err := into.UnmarshalCBOR(data); err != nil {
			return err
		}
		*node = *conv.ToDrawingPtr(into)
		return nil
	},
	QueryOne: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) (*model.Drawing, error) {
		raw, err := dbQueryOne[conv.Drawing](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToDrawingPtr(raw), nil
	},
	ReadOne: func(ctx context.Context, db *dbConn, id *models.RecordID) (*model.Drawing, error) {
		raw, err := dbSelect[conv.Drawing](ctx, db, id)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToDrawingPtr(raw), nil
	},
	UpdateOne: func(ctx context.Context, db *dbConn, id *models.RecordID, data any) (*model.Drawing, error) {
		raw, err := dbUpdate[conv.Drawing](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToDrawingPtr(raw), nil
	},
}

// DrawingRepo returns the repository instance for the Drawing model.
// The instance is cached as a singleton on the client.
func (c *ClientImpl) DrawingRepo() DrawingRepo {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.drawingRepo == nil {
		c.drawingRepo = &drawing{repo: &repo[model.Drawing, string]{
			db:     c.db,
			name:   "drawing",
			info:   drawingRepoInfo,
			autoID: true,
			recordID: func(id string) *models.RecordID {
				rid := models.NewRecordID("drawing", parseStringID(id))
				return &rid
			},
		}}
	}
	return c.drawingRepo
}

type drawing struct {
	*repo[model.Drawing, string]
}

// Query returns a new query builder for the Drawing model.
func (r *drawing) Query() query.Builder[model.Drawing] {
	return query.NewDrawing(r.db)
}

// Create creates a new record for the Drawing model.
// The ID will be generated automatically as a ULID.
// Before- and after-create hooks are invoked.
func (r *drawing) Create(ctx context.Context, drawing *model.Drawing) error {
	if drawing == nil {
		return errors.New("the passed node must not be nil")
	}
	if drawing.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, drawing); err != nil {
		return err
	}
	if err := r.create(ctx, drawing); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, drawing); err != nil {
		return err
	}
	return nil
}

// CreateWithID creates a new record for the Drawing model with the given id.
// Before- and after-create hooks are invoked.
func (r *drawing) CreateWithID(ctx context.Context, id string, drawing *model.Drawing) error {
	if drawing == nil {
		return errors.New("the passed node must not be nil")
	}
	if id == "" {
		return som.ErrEmptyID
	}
	if drawing.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, drawing); err != nil {
		return err
	}
	if err := r.createWithID(ctx, id, drawing); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, drawing); err != nil {
		return err
	}
	return nil
}

// Insert creates multiple records in a single operation.
// Before- and after-create hooks are invoked for each node.
func (r *drawing) Insert(ctx context.Context, nodes []*model.Drawing) error {
	if len(nodes) == 0 {
		return nil
	}
	for _, n := range nodes {
		if n == nil {
			return errors.New("slice contains nil node")
		}
		if n.ID() != "" {
			return errors.New("node already has an id")
		}
	}
	if err := r.runHooksAll(ctx, beforeCreate, nodes); err != nil {
		return err
	}
	if err := r.insert(ctx, nodes); err != nil {
		return err
	}
	if err := r.runHooksAll(ctx, afterCreate, nodes); err != nil {
		return err
	}
	return nil
}

// Read returns the record for the given id, if it exists.
// The returned bool indicates whether the record was found or not.
// If caching is enabled via som.WithCache, it will be used.
func (r *drawing) Read(ctx context.Context, id string) (*model.Drawing, bool, error) {
	if id == "" {
		return nil, false, som.ErrEmptyID
	}
	rid := r.recordID(id)
	if internal.TxActive(ctx) {
		return r.read(ctx, rid)
	}
	if !internal.CacheEnabled[model.Drawing](ctx) {
		return r.read(ctx, rid)
	}
	idFunc := func(n *model.Drawing) string {
		return string(n.ID())
	}
	queryAll := func(ctx context.Context) ([]*model.Drawing, error) {
		return r.Query().All(ctx)
	}
	countAll := func(ctx context.Context) (int, error) {
		return r.Query().Count(ctx)
	}
	cache, err := getOrCreateCache[model.Drawing](ctx, idFunc, queryAll, countAll)
	if err != nil {
		return nil, false, err
	}
	var refreshFuncs *eagerRefreshFuncs[model.Drawing]
	if cache != nil && cache.isEager() {
		refreshFuncs = &eagerRefreshFuncs[model.Drawing]{cacheID: internal.GetCacheKey[model.Drawing](ctx), queryAll: queryAll, countAll: countAll, idFunc: idFunc}
	}
	return r.readWithCache(ctx, id, rid, cache, refreshFuncs)
}

// Update updates the record for the given model.
// Before- and after-update hooks are invoked.
func (r *drawing) Update(ctx context.Context, drawing *model.Drawing) error {
	if drawing == nil {
		return errors.New("the passed node must not be nil")
	}
	if drawing.IsPartial() {
		return som.ErrPartialModel
	}
	if drawing.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if drawing.ID() == "" {
		return errors.New("cannot update Drawing without existing record ID")
	}
	if err := r.runHooks(ctx, beforeUpdate, drawing); err != nil {
		return err
	}
	if err := r.update(ctx, r.recordID(string(drawing.ID())), drawing); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterUpdate, drawing); err != nil {
		return err
	}
	return nil
}

// Delete deletes the record for the given model.
// Before- and after-delete hooks are invoked.
func (r *drawing) Delete(ctx context.Context, drawing *model.Drawing) error {
	if drawing == nil {
		return errors.New("the passed node must not be nil")
	}
	if drawing.IsPartial() {
		return som.ErrPartialModel
	}
	if drawing.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if drawing.ID() == "" {
		return errors.New("cannot delete Drawing without existing record ID")
	}
	if err := r.runHooks(ctx, beforeDelete, drawing); err != nil {
		return err
	}
	if err := r.delete(ctx, r.recordID(string(drawing.ID())), drawing, false, nil); err != nil {
		return err
	}
	internal.AddMarker(&drawing.Node, internal.MarkerDeleted)
	if err := r.runHooks(ctx, afterDelete, drawing); err != nil {
		return err
	}
	return nil
}

// Refresh refreshes the given model with the remote data.
func (r *drawing) Refresh(ctx context.Context, drawing *model.Drawing) error {
	if drawing == nil {
		return errors.New("the passed node must not be nil")
	}
	if drawing.ID() == "" {
		return errors.New("cannot refresh Drawing without existing record ID")
	}
	return r.refresh(ctx, r.recordID(string(drawing.ID())), drawing)
}

// Resolve loads the given relations of the model from the database. The model is
// updated in-place, so a resolved relation is no longer marked as partial and
// IsPartial reports whether it was loaded.
//
// Note that a loaded relation is not necessarily up to date. Resolve only makes
// sure the relation holds field values at all, it does not re-read a relation
// that was already loaded. Use Refresh to get current data.
func (r *drawing) Resolve(ctx context.Context, drawing *model.Drawing, fetch ...with.Fetch_[model.Drawing]) error {
	if drawing == nil {
		return errors.New("the passed node must not be nil")
	}
	if drawing.ID() == "" {
		return errors.New("cannot resolve Drawing without existing record ID")
	}
	// Relations that are already loaded are not requested again.
	var paths []string
	for _, f := range fetch {
		path := fmt.Sprintf("%v", f)
		if path == "" || conv.DrawingResolved(drawing, path) || slices.Contains(paths, path) {
			continue
		}
		paths = append(paths, path)
	}
	return r.resolve(ctx, r.recordID(string(drawing.ID())), drawing, paths)
}

// Relate returns a new relate instance for the Drawing model.
func (r *drawing) Relate() *relate.Drawing {
	return relate.NewDrawing(r.db)
}

// Index returns a new index instance for the Drawing model.
func (r *drawing) Index() *index.Drawing {
	return index.NewDrawing(r.db)
}
