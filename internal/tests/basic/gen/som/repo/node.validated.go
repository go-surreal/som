// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package repo

import (
	"context"
	"errors"
	"fmt"
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	"slices"
	som "som.test/gen/som"
	conv "som.test/gen/som/conv"
	index "som.test/gen/som/index"
	internal "som.test/gen/som/internal"
	query "som.test/gen/som/query"
	relate "som.test/gen/som/relate"
	validate "som.test/gen/som/validate"
	with "som.test/gen/som/with"
	model "som.test/model"
)

type ValidatedRepo interface {
	// Query returns a new query builder for the Validated model.
	Query() query.Builder[model.Validated]
	// Create creates a new record for the Validated model.
	Create(ctx context.Context, validated *model.Validated) error
	// Insert creates multiple records in a single operation.
	// Before- and after-create hooks are invoked for each node.
	Insert(ctx context.Context, nodes []*model.Validated) error
	// CreateWithID creates a new record with the given ID for the Validated model.
	CreateWithID(ctx context.Context, id string, validated *model.Validated) error
	// Read returns the record for the given ID, if it exists.
	Read(ctx context.Context, id string) (*model.Validated, bool, error)
	// Update updates the record for the given Validated model.
	Update(ctx context.Context, validated *model.Validated) error
	// Delete deletes the record for the given Validated model.
	Delete(ctx context.Context, validated *model.Validated) error
	// Refresh refreshes the given model with the current database state.
	Refresh(ctx context.Context, validated *model.Validated) error
	// Relate returns a new relate builder for the Validated model.
	Relate() *relate.Validated
	// Resolve loads the given relations of the model from the database.
	Resolve(ctx context.Context, validated *model.Validated, fetch ...with.Fetch_[model.Validated]) error
	// Index returns a new index instance for the Validated model.
	Index() *index.Validated

	// OnBeforeCreate registers a hook that runs before a record is created.
	// If the hook returns an error, the create operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeCreate(fn func(ctx context.Context, node *model.Validated) error) func()
	// OnAfterCreate registers a hook that runs after a record has been created.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterCreate(fn func(ctx context.Context, node *model.Validated) error) func()
	// OnBeforeUpdate registers a hook that runs before a record is updated.
	// If the hook returns an error, the update operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeUpdate(fn func(ctx context.Context, node *model.Validated) error) func()
	// OnAfterUpdate registers a hook that runs after a record has been updated.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterUpdate(fn func(ctx context.Context, node *model.Validated) error) func()
	// OnBeforeDelete registers a hook that runs before a record is deleted.
	// If the hook returns an error, the delete operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeDelete(fn func(ctx context.Context, node *model.Validated) error) func()
	// OnAfterDelete registers a hook that runs after a record has been deleted.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterDelete(fn func(ctx context.Context, node *model.Validated) error) func()
}

// validatedRepoInfo holds the model-specific conversion functions for Validated.
var validatedRepoInfo = RepoInfo[model.Validated]{
	CreateNew: func(ctx context.Context, db *dbConn, target string, data any) (*model.Validated, error) {
		raw, err := dbCreateNew[conv.Validated](ctx, db, target, data)
		if err != nil {
			return nil, err
		}
		return conv.ToValidatedPtr(raw), nil
	},
	CreateOne: func(ctx context.Context, db *dbConn, id models.RecordID, data any) (*model.Validated, error) {
		raw, err := dbCreate[conv.Validated](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToValidatedPtr(raw), nil
	},
	InsertAll: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) ([]*model.Validated, error) {
		raw, err := dbInsert[conv.Validated](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		results := make([]*model.Validated, len(raw))
		for i, r := range raw {
			results[i] = conv.ToValidatedPtr(r)
		}
		return results, nil
	},
	MarshalOne: func(node *model.Validated) any {
		return conv.FromValidatedPtr(node)
	},
	MergeOne: func(node *model.Validated, data []byte) error {
		into := conv.FromValidatedPtr(node)
		if err := into.UnmarshalCBOR(data); err != nil {
			return err
		}
		*node = *conv.ToValidatedPtr(into)
		return nil
	},
	QueryOne: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) (*model.Validated, error) {
		raw, err := dbQueryOne[conv.Validated](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToValidatedPtr(raw), nil
	},
	ReadOne: func(ctx context.Context, db *dbConn, id *models.RecordID) (*model.Validated, error) {
		raw, err := dbSelect[conv.Validated](ctx, db, id)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToValidatedPtr(raw), nil
	},
	UpdateOne: func(ctx context.Context, db *dbConn, id *models.RecordID, data any) (*model.Validated, error) {
		raw, err := dbUpdate[conv.Validated](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToValidatedPtr(raw), nil
	},
}

// ValidatedRepo returns the repository instance for the Validated model.
// The instance is cached as a singleton on the client.
func (c *ClientImpl) ValidatedRepo() ValidatedRepo {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.validatedRepo == nil {
		c.validatedRepo = &validated{repo: &repo[model.Validated, string]{
			db:     c.db,
			name:   "validated",
			info:   validatedRepoInfo,
			autoID: true,
			recordID: func(id string) *models.RecordID {
				rid := models.NewRecordID("validated", parseStringID(id))
				return &rid
			},
		}}
	}
	return c.validatedRepo
}

type validated struct {
	*repo[model.Validated, string]
}

// Query returns a new query builder for the Validated model.
func (r *validated) Query() query.Builder[model.Validated] {
	return query.NewValidated(r.db)
}

// Create creates a new record for the Validated model.
// The ID will be generated automatically as a ULID.
// Before- and after-create hooks are invoked.
func (r *validated) Create(ctx context.Context, validated *model.Validated) error {
	if validated == nil {
		return errors.New("the passed node must not be nil")
	}
	if validated.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, validated); err != nil {
		return err
	}
	if err := validate.Validated(validated); err != nil {
		return err
	}
	if err := r.create(ctx, validated); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, validated); err != nil {
		return err
	}
	return nil
}

// CreateWithID creates a new record for the Validated model with the given id.
// Before- and after-create hooks are invoked.
func (r *validated) CreateWithID(ctx context.Context, id string, validated *model.Validated) error {
	if validated == nil {
		return errors.New("the passed node must not be nil")
	}
	if id == "" {
		return som.ErrEmptyID
	}
	if validated.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, validated); err != nil {
		return err
	}
	if err := validate.Validated(validated); err != nil {
		return err
	}
	if err := r.createWithID(ctx, id, validated); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, validated); err != nil {
		return err
	}
	return nil
}

// Insert creates multiple records in a single operation.
// Before- and after-create hooks are invoked for each node.
func (r *validated) Insert(ctx context.Context, nodes []*model.Validated) error {
	if len(nodes) == 0 {
		return nil
	}
	for _, n := range nodes {
		if n == nil {
			return errors.New("slice contains nil node")
		}
		if n.ID() != "" {
			return errors.New("node already has an id")
		}
	}
	if err := r.runHooksAll(ctx, beforeCreate, nodes); err != nil {
		return err
	}
	for _, n := range nodes {
		if err := validate.Validated(n); err != nil {
			return err
		}
	}
	if err := r.insert(ctx, nodes); err != nil {
		return err
	}
	if err := r.runHooksAll(ctx, afterCreate, nodes); err != nil {
		return err
	}
	return nil
}

// Read returns the record for the given id, if it exists.
// The returned bool indicates whether the record was found or not.
// If caching is enabled via som.WithCache, it will be used.
func (r *validated) Read(ctx context.Context, id string) (*model.Validated, bool, error) {
	if id == "" {
		return nil, false, som.ErrEmptyID
	}
	rid := r.recordID(id)
	if internal.TxActive(ctx) {
		return r.read(ctx, rid)
	}
	if !internal.CacheEnabled[model.Validated](ctx) {
		return r.read(ctx, rid)
	}
	idFunc := func(n *model.Validated) string {
		return string(n.ID())
	}
	queryAll := func(ctx context.Context) ([]*model.Validated, error) {
		return r.Query().All(ctx)
	}
	countAll := func(ctx context.Context) (int, error) {
		return r.Query().Count(ctx)
	}
	cache, err := getOrCreateCache[model.Validated](ctx, idFunc, queryAll, countAll)
	if err != nil {
		return nil, false, err
	}
	var refreshFuncs *eagerRefreshFuncs[model.Validated]
	if cache != nil && cache.isEager() {
		refreshFuncs = &eagerRefreshFuncs[model.Validated]{cacheID: internal.GetCacheKey[model.Validated](ctx), queryAll: queryAll, countAll: countAll, idFunc: idFunc}
	}
	return r.readWithCache(ctx, id, rid, cache, refreshFuncs)
}

// Update updates the record for the given model.
// Before- and after-update hooks are invoked.
func (r *validated) Update(ctx context.Context, validated *model.Validated) error {
	if validated == nil {
		return errors.New("the passed node must not be nil")
	}
	if validated.IsPartial() {
		return som.ErrPartialModel
	}
	if validated.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if validated.ID() == "" {
		return errors.New("cannot update Validated without existing record ID")
	}
	if err := r.runHooks(ctx, beforeUpdate, validated); err != nil {
		return err
	}
	if err := validate.Validated(validated); err != nil {
		return err
	}
	if err := r.update(ctx, r.recordID(string(validated.ID())), validated); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterUpdate, validated); err != nil {
		return err
	}
	return nil
}

// Delete deletes the record for the given model.
// Before- and after-delete hooks are invoked.
func (r *validated) Delete(ctx context.Context, validated *model.Validated) error {
	if validated == nil {
		return errors.New("the passed node must not be nil")
	}
	if validated.IsPartial() {
		return som.ErrPartialModel
	}
	if validated.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if validated.ID() == "" {
		return errors.New("cannot delete Validated without existing record ID")
	}
	if err := r.runHooks(ctx, beforeDelete, validated); err != nil {
		return err
	}
	if err := r.delete(ctx, r.recordID(string(validated.ID())), validated, false, nil); err != nil {
		return err
	}
	internal.AddMarker(&validated.Node, internal.MarkerDeleted)
	if err := r.runHooks(ctx, afterDelete, validated); err != nil {
		return err
	}
	return nil
}

// Refresh refreshes the given model with the remote data.
func (r *validated) Refresh(ctx context.Context, validated *model.Validated) error {
	if validated == nil {
		return errors.New("the passed node must not be nil")
	}
	if validated.ID() == "" {
		return errors.New("cannot refresh Validated without existing record ID")
	}
	return r.refresh(ctx, r.recordID(string(validated.ID())), validated)
}

// Resolve loads the given relations of the model from the database. The model is
// updated in-place, so a resolved relation is no longer marked as partial and
// IsPartial reports whether it was loaded.
//
// Note that a loaded relation is not necessarily up to date. Resolve only makes
// sure the relation holds field values at all, it does not re-read a relation
// that was already loaded. Use Refresh to get current data.
func (r *validated) Resolve(ctx context.Context, validated *model.Validated, fetch ...with.Fetch_[model.Validated]) error {
	if validated == nil {
		return errors.New("the passed node must not be nil")
	}
	if validated.ID() == "" {
		return errors.New("cannot resolve Validated without existing record ID")
	}
	// Relations that are already loaded are not requested again.
	var paths []string
	for _, f := range fetch {
		path := fmt.Sprintf("%v", f)
		if path == "" || conv.ValidatedResolved(validated, path) || slices.Contains(paths, path) {
			continue
		}
		paths = append(paths, path)
	}
	return r.resolve(ctx, r.recordID(string(validated.ID())), validated, paths)
}

// Relate returns a new relate instance for the Validated model.
func (r *validated) Relate() *relate.Validated {
	return relate.NewValidated(r.db)
}

// Index returns a new index instance for the Validated model.
func (r *validated) Index() *index.Validated {
	return index.NewValidated(r.db)
}
