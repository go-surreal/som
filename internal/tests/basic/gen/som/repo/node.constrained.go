// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package repo

import (
	"context"
	"errors"
	"fmt"
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	"slices"
	som "som.test/gen/som"
	conv "som.test/gen/som/conv"
	index "som.test/gen/som/index"
	internal "som.test/gen/som/internal"
	query "som.test/gen/som/query"
	relate "som.test/gen/som/relate"
	with "som.test/gen/som/with"
	model "som.test/model"
)

type ConstrainedRepo interface {
	// Query returns a new query builder for the Constrained model.
	Query() query.Builder[model.Constrained]
	// Create creates a new record for the Constrained model.
	Create(ctx context.Context, constrained *model.Constrained) error
	// Insert creates multiple records in a single operation.
	// Before- and after-create hooks are invoked for each node.
	Insert(ctx context.Context, nodes []*model.Constrained) error
	// CreateWithID creates a new record with the given ID for the Constrained model.
	CreateWithID(ctx context.Context, id string, constrained *model.Constrained) error
	// Read returns the record for the given ID, if it exists.
	Read(ctx context.Context, id string) (*model.Constrained, bool, error)
	// Update updates the record for the given Constrained model.
	Update(ctx context.Context, constrained *model.Constrained) error
	// Delete deletes the record for the given Constrained model.
	Delete(ctx context.Context, constrained *model.Constrained) error
	// Refresh refreshes the given model with the current database state.
	Refresh(ctx context.Context, constrained *model.Constrained) error
	// Relate returns a new relate builder for the Constrained model.
	Relate() *relate.Constrained
	// Resolve loads the given relations of the model from the database.
	Resolve(ctx context.Context, constrained *model.Constrained, fetch ...with.Fetch_[model.Constrained]) error
	// Index returns a new index instance for the Constrained model.
	Index() *index.Constrained

	// OnBeforeCreate registers a hook that runs before a record is created.
	// If the hook returns an error, the create operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeCreate(fn func(ctx context.Context, node *model.Constrained) error) func()
	// OnAfterCreate registers a hook that runs after a record has been created.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterCreate(fn func(ctx context.Context, node *model.Constrained) error) func()
	// OnBeforeUpdate registers a hook that runs before a record is updated.
	// If the hook returns an error, the update operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeUpdate(fn func(ctx context.Context, node *model.Constrained) error) func()
	// OnAfterUpdate registers a hook that runs after a record has been updated.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterUpdate(fn func(ctx context.Context, node *model.Constrained) error) func()
	// OnBeforeDelete registers a hook that runs before a record is deleted.
	// If the hook returns an error, the delete operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeDelete(fn func(ctx context.Context, node *model.Constrained) error) func()
	// OnAfterDelete registers a hook that runs after a record has been deleted.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterDelete(fn func(ctx context.Context, node *model.Constrained) error) func()
}

// constrainedRepoInfo holds the model-specific conversion functions for Constrained.
var constrainedRepoInfo = RepoInfo[model.Constrained]{
	CreateNew: func(ctx context.Context, db *dbConn, target string, data any) (*model.Constrained, error) {
		raw, err := dbCreateNew[conv.Constrained](ctx, db, target, data)
		if err != nil {
			return nil, err
		}
		return conv.ToConstrainedPtr(raw), nil
	},
	CreateOne: func(ctx context.Context, db *dbConn, id models.RecordID, data any) (*model.Constrained, error) {
		raw, err := dbCreate[conv.Constrained](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToConstrainedPtr(raw), nil
	},
	InsertAll: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) ([]*model.Constrained, error) {
		raw, err := dbInsert[conv.Constrained](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		results := make([]*model.Constrained, len(raw))
		for i, r := range raw {
			results[i] = conv.ToConstrainedPtr(r)
		}
		return results, nil
	},
	MarshalOne: func(node *model.Constrained) any {
		return conv.FromConstrainedPtr(node)
	},
	MergeOne: func(node *model.Constrained, data []byte) error {
		into := conv.FromConstrainedPtr(node)
		if err := into.UnmarshalCBOR(data); err != nil {
			return err
		}
		*node = *conv.ToConstrainedPtr(into)
		return nil
	},
	QueryOne: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) (*model.Constrained, error) {
		raw, err := dbQueryOne[conv.Constrained](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToConstrainedPtr(raw), nil
	},
	ReadOne: func(ctx context.Context, db *dbConn, id *models.RecordID) (*model.Constrained, error) {
		raw, err := dbSelect[conv.Constrained](ctx, db, id)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToConstrainedPtr(raw), nil
	},
	UpdateOne: func(ctx context.Context, db *dbConn, id *models.RecordID, data any) (*model.Constrained, error) {
		raw, err := dbUpdate[conv.Constrained](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToConstrainedPtr(raw), nil
	},
}

// ConstrainedRepo returns the repository instance for the Constrained model.
// The instance is cached as a singleton on the client.
func (c *ClientImpl) ConstrainedRepo() ConstrainedRepo {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.constrainedRepo == nil {
		c.constrainedRepo = &constrained{repo: &repo[model.Constrained, string]{
			db:     c.db,
			name:   "constrained",
			info:   constrainedRepoInfo,
			autoID: true,
			recordID: func(id string) *models.RecordID {
				rid := models.NewRecordID("constrained", parseStringID(id))
				return &rid
			},
		}}
	}
	return c.constrainedRepo
}

type constrained struct {
	*repo[model.Constrained, string]
}

// Query returns a new query builder for the Constrained model.
func (r *constrained) Query() query.Builder[model.Constrained] {
	return query.NewConstrained(r.db)
}

// Create creates a new record for the Constrained model.
// The ID will be generated automatically as a ULID.
// Before- and after-create hooks are invoked.
func (r *constrained) Create(ctx context.Context, constrained *model.Constrained) error {
	if constrained == nil {
		return errors.New("the passed node must not be nil")
	}
	if constrained.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, constrained); err != nil {
		return err
	}
	if err := r.create(ctx, constrained); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, constrained); err != nil {
		return err
	}
	return nil
}

// CreateWithID creates a new record for the Constrained model with the given id.
// Before- and after-create hooks are invoked.
func (r *constrained) CreateWithID(ctx context.Context, id string, constrained *model.Constrained) error {
	if constrained == nil {
		return errors.New("the passed node must not be nil")
	}
	if id == "" {
		return som.ErrEmptyID
	}
	if constrained.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, constrained); err != nil {
		return err
	}
	if err := r.createWithID(ctx, id, constrained); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, constrained); err != nil {
		return err
	}
	return nil
}

// Insert creates multiple records in a single operation.
// Before- and after-create hooks are invoked for each node.
func (r *constrained) Insert(ctx context.Context, nodes []*model.Constrained) error {
	if len(nodes) == 0 {
		return nil
	}
	for _, n := range nodes {
		if n == nil {
			return errors.New("slice contains nil node")
		}
		if n.ID() != "" {
			return errors.New("node already has an id")
		}
	}
	if err := r.runHooksAll(ctx, beforeCreate, nodes); err != nil {
		return err
	}
	if err := r.insert(ctx, nodes); err != nil {
		return err
	}
	if err := r.runHooksAll(ctx, afterCreate, nodes); err != nil {
		return err
	}
	return nil
}

// Read returns the record for the given id, if it exists.
// The returned bool indicates whether the record was found or not.
// If caching is enabled via som.WithCache, it will be used.
func (r *constrained) Read(ctx context.Context, id string) (*model.Constrained, bool, error) {
	if id == "" {
		return nil, false, som.ErrEmptyID
	}
	rid := r.recordID(id)
	if internal.TxActive(ctx) {
		return r.read(ctx, rid)
	}
	if !internal.CacheEnabled[model.Constrained](ctx) {
		return r.read(ctx, rid)
	}
	idFunc := func(n *model.Constrained) string {
		return string(n.ID())
	}
	queryAll := func(ctx context.Context) ([]*model.Constrained, error) {
		return r.Query().All(ctx)
	}
	countAll := func(ctx context.Context) (int, error) {
		return r.Query().Count(ctx)
	}
	cache, err := getOrCreateCache[model.Constrained](ctx, idFunc, queryAll, countAll)
	if err != nil {
		return nil, false, err
	}
	var refreshFuncs *eagerRefreshFuncs[model.Constrained]
	if cache != nil && cache.isEager() {
		refreshFuncs = &eagerRefreshFuncs[model.Constrained]{cacheID: internal.GetCacheKey[model.Constrained](ctx), queryAll: queryAll, countAll: countAll, idFunc: idFunc}
	}
	return r.readWithCache(ctx, id, rid, cache, refreshFuncs)
}

// Update updates the record for the given model.
// Before- and after-update hooks are invoked.
func (r *constrained) Update(ctx context.Context, constrained *model.Constrained) error {
	if constrained == nil {
		return errors.New("the passed node must not be nil")
	}
	if constrained.IsPartial() {
		return som.ErrPartialModel
	}
	if constrained.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if constrained.ID() == "" {
		return errors.New("cannot update Constrained without existing record ID")
	}
	if err := r.runHooks(ctx, beforeUpdate, constrained); err != nil {
		return err
	}
	if err := r.update(ctx, r.recordID(string(constrained.ID())), constrained); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterUpdate, constrained); err != nil {
		return err
	}
	return nil
}

// Delete deletes the record for the given model.
// Before- and after-delete hooks are invoked.
func (r *constrained) Delete(ctx context.Context, constrained *model.Constrained) error {
	if constrained == nil {
		return errors.New("the passed node must not be nil")
	}
	if constrained.IsPartial() {
		return som.ErrPartialModel
	}
	if constrained.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if constrained.ID() == "" {
		return errors.New("cannot delete Constrained without existing record ID")
	}
	if err := r.runHooks(ctx, beforeDelete, constrained); err != nil {
		return err
	}
	if err := r.delete(ctx, r.recordID(string(constrained.ID())), constrained, false, nil); err != nil {
		return err
	}
	internal.AddMarker(&constrained.Node, internal.MarkerDeleted)
	if err := r.runHooks(ctx, afterDelete, constrained); err != nil {
		return err
	}
	return nil
}

// Refresh refreshes the given model with the remote data.
func (r *constrained) Refresh(ctx context.Context, constrained *model.Constrained) error {
	if constrained == nil {
		return errors.New("the passed node must not be nil")
	}
	if constrained.ID() == "" {
		return errors.New("cannot refresh Constrained without existing record ID")
	}
	return r.refresh(ctx, r.recordID(string(constrained.ID())), constrained)
}

// Resolve loads the given relations of the model from the database. The model is
// updated in-place, so a resolved relation is no longer marked as partial and
// IsPartial reports whether it was loaded.
//
// Note that a loaded relation is not necessarily up to date. Resolve only makes
// sure the relation holds field values at all, it does not re-read a relation
// that was already loaded. Use Refresh to get current data.
func (r *constrained) Resolve(ctx context.Context, constrained *model.Constrained, fetch ...with.Fetch_[model.Constrained]) error {
	if constrained == nil {
		return errors.New("the passed node must not be nil")
	}
	if constrained.ID() == "" {
		return errors.New("cannot resolve Constrained without existing record ID")
	}
	// Relations that are already loaded are not requested again.
	var paths []string
	for _, f := range fetch {
		path := fmt.Sprintf("%v", f)
		if path == "" || conv.ConstrainedResolved(constrained, path) || slices.Contains(paths, path) {
			continue
		}
		paths = append(paths, path)
	}
	return r.resolve(ctx, r.recordID(string(constrained.ID())), constrained, paths)
}

// Relate returns a new relate instance for the Constrained model.
func (r *constrained) Relate() *relate.Constrained {
	return relate.NewConstrained(r.db)
}

// Index returns a new index instance for the Constrained model.
func (r *constrained) Index() *index.Constrained {
	return index.NewConstrained(r.db)
}
