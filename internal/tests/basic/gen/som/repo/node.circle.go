// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package repo

import (
	"context"
	"errors"
	"fmt"
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	"slices"
	som "som.test/gen/som"
	conv "som.test/gen/som/conv"
	index "som.test/gen/som/index"
	internal "som.test/gen/som/internal"
	query "som.test/gen/som/query"
	relate "som.test/gen/som/relate"
	with "som.test/gen/som/with"
	model "som.test/model"
)

type CircleRepo interface {
	// Query returns a new query builder for the Circle model.
	Query() query.Builder[model.Circle]
	// Create creates a new record for the Circle model.
	Create(ctx context.Context, circle *model.Circle) error
	// Insert creates multiple records in a single operation.
	// Before- and after-create hooks are invoked for each node.
	Insert(ctx context.Context, nodes []*model.Circle) error
	// CreateWithID creates a new record with the given ID for the Circle model.
	CreateWithID(ctx context.Context, id string, circle *model.Circle) error
	// Read returns the record for the given ID, if it exists.
	Read(ctx context.Context, id string) (*model.Circle, bool, error)
	// Update updates the record for the given Circle model.
	Update(ctx context.Context, circle *model.Circle) error
	// Delete deletes the record for the given Circle model.
	Delete(ctx context.Context, circle *model.Circle) error
	// Refresh refreshes the given model with the current database state.
	Refresh(ctx context.Context, circle *model.Circle) error
	// Relate returns a new relate builder for the Circle model.
	Relate() *relate.Circle
	// Resolve loads the given relations of the model from the database.
	Resolve(ctx context.Context, circle *model.Circle, fetch ...with.Fetch_[model.Circle]) error
	// Index returns a new index instance for the Circle model.
	Index() *index.Circle

	// OnBeforeCreate registers a hook that runs before a record is created.
	// If the hook returns an error, the create operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeCreate(fn func(ctx context.Context, node *model.Circle) error) func()
	// OnAfterCreate registers a hook that runs after a record has been created.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterCreate(fn func(ctx context.Context, node *model.Circle) error) func()
	// OnBeforeUpdate registers a hook that runs before a record is updated.
	// If the hook returns an error, the update operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeUpdate(fn func(ctx context.Context, node *model.Circle) error) func()
	// OnAfterUpdate registers a hook that runs after a record has been updated.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterUpdate(fn func(ctx context.Context, node *model.Circle) error) func()
	// OnBeforeDelete registers a hook that runs before a record is deleted.
	// If the hook returns an error, the delete operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeDelete(fn func(ctx context.Context, node *model.Circle) error) func()
	// OnAfterDelete registers a hook that runs after a record has been deleted.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterDelete(fn func(ctx context.Context, node *model.Circle) error) func()
}

// circleRepoInfo holds the model-specific conversion functions for Circle.
var circleRepoInfo = RepoInfo[model.Circle]{
	CreateNew: func(ctx context.Context, db *dbConn, target string, data any) (*model.Circle, error) {
		raw, err := dbCreateNew[conv.Circle](ctx, db, target, data)
		if err != nil {
			return nil, err
		}
		return conv.ToCirclePtr(raw), nil
	},
	CreateOne: func(ctx context.Context, db *dbConn, id models.RecordID, data any) (*model.Circle, error) {
		raw, err := dbCreate[conv.Circle](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToCirclePtr(raw), nil
	},
	InsertAll: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) ([]*model.Circle, error) {
		raw, err := dbInsert[conv.Circle](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		results := make([]*model.Circle, len(raw))
		for i, r := range raw {
			results[i] = conv.ToCirclePtr(r)
		}
		return results, nil
	},
	MarshalOne: func(node *model.Circle) any {
		return conv.FromCirclePtr(node)
	},
	MergeOne: func(node *model.Circle, data []byte) error {
		into := conv.FromCirclePtr(node)
		if err := into.UnmarshalCBOR(data); err != nil {
			return err
		}
		*node = *conv.ToCirclePtr(into)
		return nil
	},
	QueryOne: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) (*model.Circle, error) {
		raw, err := dbQueryOne[conv.Circle](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToCirclePtr(raw), nil
	},
	ReadOne: func(ctx context.Context, db *dbConn, id *models.RecordID) (*model.Circle, error) {
		raw, err := dbSelect[conv.Circle](ctx, db, id)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToCirclePtr(raw), nil
	},
	UpdateOne: func(ctx context.Context, db *dbConn, id *models.RecordID, data any) (*model.Circle, error) {
		raw, err := dbUpdate[conv.Circle](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToCirclePtr(raw), nil
	},
}

// CircleRepo returns the repository instance for the Circle model.
// The instance is cached as a singleton on the client.
func (c *ClientImpl) CircleRepo() CircleRepo {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.circleRepo == nil {
		c.circleRepo = &circle{repo: &repo[model.Circle, string]{
			db:     c.db,
			name:   "circle",
			info:   circleRepoInfo,
			autoID: true,
			recordID: func(id string) *models.RecordID {
				rid := models.NewRecordID("circle", parseUUID(id))
				return &rid
			},
		}}
	}
	return c.circleRepo
}

type circle struct {
	*repo[model.Circle, string]
}

// Query returns a new query builder for the Circle model.
func (r *circle) Query() query.Builder[model.Circle] {
	return query.NewCircle(r.db)
}

// Create creates a new record for the Circle model.
// The ID will be generated automatically as a UUID.
// Before- and after-create hooks are invoked.
func (r *circle) Create(ctx context.Context, circle *model.Circle) error {
	if circle == nil {
		return errors.New("the passed node must not be nil")
	}
	if circle.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, circle); err != nil {
		return err
	}
	if err := r.create(ctx, circle); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, circle); err != nil {
		return err
	}
	return nil
}

// CreateWithID creates a new record for the Circle model with the given id.
// Before- and after-create hooks are invoked.
func (r *circle) CreateWithID(ctx context.Context, id string, circle *model.Circle) error {
	if circle == nil {
		return errors.New("the passed node must not be nil")
	}
	if id == "" {
		return som.ErrEmptyID
	}
	if circle.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, circle); err != nil {
		return err
	}
	if err := r.createWithID(ctx, id, circle); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, circle); err != nil {
		return err
	}
	return nil
}

// Insert creates multiple records in a single operation.
// Before- and after-create hooks are invoked for each node.
func (r *circle) Insert(ctx context.Context, nodes []*model.Circle) error {
	if len(nodes) == 0 {
		return nil
	}
	for _, n := range nodes {
		if n == nil {
			return errors.New("slice contains nil node")
		}
		if n.ID() != "" {
			return errors.New("node already has an id")
		}
	}
	if err := r.runHooksAll(ctx, beforeCreate, nodes); err != nil {
		return err
	}
	if err := r.insert(ctx, nodes); err != nil {
		return err
	}
	if err := r.runHooksAll(ctx, afterCreate, nodes); err != nil {
		return err
	}
	return nil
}

// Read returns the record for the given id, if it exists.
// The returned bool indicates whether the record was found or not.
// If caching is enabled via som.WithCache, it will be used.
func (r *circle) Read(ctx context.Context, id string) (*model.Circle, bool, error) {
	if id == "" {
		return nil, false, som.ErrEmptyID
	}
	rid := r.recordID(id)
	if internal.TxActive(ctx) {
		return r.read(ctx, rid)
	}
	if !internal.CacheEnabled[model.Circle](ctx) {
		return r.read(ctx, rid)
	}
	idFunc := func(n *model.Circle) string {
		return string(n.ID())
	}
	queryAll := func(ctx context.Context) ([]*model.Circle, error) {
		return r.Query().All(ctx)
	}
	countAll := func(ctx context.Context) (int, error) {
		return r.Query().Count(ctx)
	}
	cache, err := getOrCreateCache[model.Circle](ctx, idFunc, queryAll, countAll)
	if err != nil {
		return nil, false, err
	}
	var refreshFuncs *eagerRefreshFuncs[model.Circle]
	if cache != nil && cache.isEager() {
		refreshFuncs = &eagerRefreshFuncs[model.Circle]{cacheID: internal.GetCacheKey[model.Circle](ctx), queryAll: queryAll, countAll: countAll, idFunc: idFunc}
	}
	return r.readWithCache(ctx, id, rid, cache, refreshFuncs)
}

// Update updates the record for the given model.
// Before- and after-update hooks are invoked.
func (r *circle) Update(ctx context.Context, circle *model.Circle) error {
	if circle == nil {
		return errors.New("the passed node must not be nil")
	}
	if circle.IsPartial() {
		return som.ErrPartialModel
	}
	if circle.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if circle.ID() == "" {
		return errors.New("cannot update Circle without existing record ID")
	}
	if err := r.runHooks(ctx, beforeUpdate, circle); err != nil {
		return err
	}
	if err := r.update(ctx, r.recordID(string(circle.ID())), circle); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterUpdate, circle); err != nil {
		return err
	}
	return nil
}

// Delete deletes the record for the given model.
// Before- and after-delete hooks are invoked.
func (r *circle) Delete(ctx context.Context, circle *model.Circle) error {
	if circle == nil {
		return errors.New("the passed node must not be nil")
	}
	if circle.IsPartial() {
		return som.ErrPartialModel
	}
	if circle.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if circle.ID() == "" {
		return errors.New("cannot delete Circle without existing record ID")
	}
	if err := r.runHooks(ctx, beforeDelete, circle); err != nil {
		return err
	}
	if err := r.delete(ctx, r.recordID(string(circle.ID())), circle, false, nil); err != nil {
		return err
	}
	internal.AddMarker(&circle.Node, internal.MarkerDeleted)
	if err := r.runHooks(ctx, afterDelete, circle); err != nil {
		return err
	}
	return nil
}

// Refresh refreshes the given model with the remote data.
func (r *circle) Refresh(ctx context.Context, circle *model.Circle) error {
	if circle == nil {
		return errors.New("the passed node must not be nil")
	}
	if circle.ID() == "" {
		return errors.New("cannot refresh Circle without existing record ID")
	}
	return r.refresh(ctx, r.recordID(string(circle.ID())), circle)
}

// Resolve loads the given relations of the model from the database. The model is
// updated in-place, so a resolved relation is no longer marked as partial and
// IsPartial reports whether it was loaded.
//
// Note that a loaded relation is not necessarily up to date. Resolve only makes
// sure the relation holds field values at all, it does not re-read a relation
// that was already loaded. Use Refresh to get current data.
func (r *circle) Resolve(ctx context.Context, circle *model.Circle, fetch ...with.Fetch_[model.Circle]) error {
	if circle == nil {
		return errors.New("the passed node must not be nil")
	}
	if circle.ID() == "" {
		return errors.New("cannot resolve Circle without existing record ID")
	}
	// Relations that are already loaded are not requested again.
	var paths []string
	for _, f := range fetch {
		path := fmt.Sprintf("%v", f)
		if path == "" || conv.CircleResolved(circle, path) || slices.Contains(paths, path) {
			continue
		}
		paths = append(paths, path)
	}
	return r.resolve(ctx, r.recordID(string(circle.ID())), circle, paths)
}

// Relate returns a new relate instance for the Circle model.
func (r *circle) Relate() *relate.Circle {
	return relate.NewCircle(r.db)
}

// Index returns a new index instance for the Circle model.
func (r *circle) Index() *index.Circle {
	return index.NewCircle(r.db)
}
