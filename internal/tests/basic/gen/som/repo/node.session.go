// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package repo

import (
	"context"
	"errors"
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	som "som.test/gen/som"
	conv "som.test/gen/som/conv"
	index "som.test/gen/som/index"
	internal "som.test/gen/som/internal"
	query "som.test/gen/som/query"
	relate "som.test/gen/som/relate"
	model "som.test/model"
)

type SessionRepo interface {
	// Query returns a new query builder for the Session model.

	Query() query.Builder[model.Session]
	// Create creates a new record for the Session model.

	Create(ctx context.Context, session *model.Session) error
	// Insert creates multiple records in a single operation.
	// Before- and after-create hooks are invoked for each node.

	Insert(ctx context.Context, nodes []*model.Session) error
	// CreateWithID creates a new record with the given ID for the Session model.

	CreateWithID(ctx context.Context, id string, session *model.Session) error
	// Read returns the record for the given ID, if it exists.

	Read(ctx context.Context, id string) (*model.Session, bool, error)
	// Update updates the record for the given Session model.

	Update(ctx context.Context, session *model.Session) error
	// Delete deletes the record for the given Session model.

	Delete(ctx context.Context, session *model.Session) error
	// Refresh refreshes the given model with the current database state.

	Refresh(ctx context.Context, session *model.Session) error
	// Relate returns a new relate builder for the Session model.

	Relate() *relate.Session
	// Index returns a new index instance for the Session model.

	Index() *index.Session

	// OnBeforeCreate registers a hook that runs before a record is created.
	// If the hook returns an error, the create operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeCreate(fn func(ctx context.Context, node *model.Session) error) func()
	// OnAfterCreate registers a hook that runs after a record has been created.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterCreate(fn func(ctx context.Context, node *model.Session) error) func()
	// OnBeforeUpdate registers a hook that runs before a record is updated.
	// If the hook returns an error, the update operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeUpdate(fn func(ctx context.Context, node *model.Session) error) func()
	// OnAfterUpdate registers a hook that runs after a record has been updated.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterUpdate(fn func(ctx context.Context, node *model.Session) error) func()
	// OnBeforeDelete registers a hook that runs before a record is deleted.
	// If the hook returns an error, the delete operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeDelete(fn func(ctx context.Context, node *model.Session) error) func()
	// OnAfterDelete registers a hook that runs after a record has been deleted.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterDelete(fn func(ctx context.Context, node *model.Session) error) func()
}

// sessionRepoInfo holds the model-specific conversion functions for Session.
var sessionRepoInfo = RepoInfo[model.Session]{
	CreateNew: func(ctx context.Context, db *dbConn, target string, data any) (*model.Session, error) {
		raw, err := dbCreateNew[conv.Session](ctx, db, target, data)
		if err != nil {
			return nil, err
		}
		return conv.ToSessionPtr(raw), nil
	},
	CreateOne: func(ctx context.Context, db *dbConn, id models.RecordID, data any) (*model.Session, error) {
		raw, err := dbCreate[conv.Session](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToSessionPtr(raw), nil
	},
	InsertAll: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) ([]*model.Session, error) {
		raw, err := dbInsert[conv.Session](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		results := make([]*model.Session, len(raw))
		for i, r := range raw {
			results[i] = conv.ToSessionPtr(r)
		}
		return results, nil
	},
	MarshalOne: func(node *model.Session) any {
		return conv.FromSessionPtr(node)
	},
	QueryOne: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) (*model.Session, error) {
		raw, err := dbQueryOne[conv.Session](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToSessionPtr(raw), nil
	},
	ReadOne: func(ctx context.Context, db *dbConn, id *models.RecordID) (*model.Session, error) {
		raw, err := dbSelect[conv.Session](ctx, db, id)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToSessionPtr(raw), nil
	},
	UpdateOne: func(ctx context.Context, db *dbConn, id *models.RecordID, data any) (*model.Session, error) {
		raw, err := dbUpdate[conv.Session](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToSessionPtr(raw), nil
	},
}

// SessionRepo returns the repository instance for the Session model.
// The instance is cached as a singleton on the client.
func (c *ClientImpl) SessionRepo() SessionRepo {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.sessionRepo == nil {
		c.sessionRepo = &session{repo: &repo[model.Session, string]{
			db:     c.db,
			name:   "session",
			info:   sessionRepoInfo,
			autoID: true,
			recordID: func(id string) *models.RecordID {
				rid := models.NewRecordID("session", parseUUID(id))
				return &rid
			}}}
	}
	return c.sessionRepo
}

type session struct {
	*repo[model.Session, string]
}

// Query returns a new query builder for the Session model.
func (r *session) Query() query.Builder[model.Session] {
	return query.NewSession(r.db)
}

// Create creates a new record for the Session model.
// The ID will be generated automatically as a ULID.
// Before- and after-create hooks are invoked.
func (r *session) Create(ctx context.Context, session *model.Session) error {
	if session == nil {
		return errors.New("the passed node must not be nil")
	}
	if session.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, session); err != nil {
		return err
	}
	if err := r.create(ctx, session); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, session); err != nil {
		return err
	}
	return nil
}

// CreateWithID creates a new record for the Session model with the given id.
// Before- and after-create hooks are invoked.
func (r *session) CreateWithID(ctx context.Context, id string, session *model.Session) error {
	if session == nil {
		return errors.New("the passed node must not be nil")
	}
	if id == "" {
		return som.ErrEmptyID
	}
	if session.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, session); err != nil {
		return err
	}
	if err := r.createWithID(ctx, id, session); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, session); err != nil {
		return err
	}
	return nil
}

// Insert creates multiple records in a single operation.
// Before- and after-create hooks are invoked for each node.
func (r *session) Insert(ctx context.Context, nodes []*model.Session) error {
	if len(nodes) == 0 {
		return nil
	}
	for _, n := range nodes {
		if n == nil {
			return errors.New("slice contains nil node")
		}
		if n.ID() != "" {
			return errors.New("node already has an id")
		}
	}
	if err := r.runHooksAll(ctx, beforeCreate, nodes); err != nil {
		return err
	}
	if err := r.insert(ctx, nodes); err != nil {
		return err
	}
	if err := r.runHooksAll(ctx, afterCreate, nodes); err != nil {
		return err
	}
	return nil
}

// Read returns the record for the given id, if it exists.
// The returned bool indicates whether the record was found or not.
// If caching is enabled via som.WithCache, it will be used.
func (r *session) Read(ctx context.Context, id string) (*model.Session, bool, error) {
	if id == "" {
		return nil, false, som.ErrEmptyID
	}
	rid := r.recordID(id)
	if internal.TxActive(ctx) {
		return r.read(ctx, rid)
	}
	if !internal.CacheEnabled[model.Session](ctx) {
		return r.read(ctx, rid)
	}
	idFunc := func(n *model.Session) string {
		return string(n.ID())
	}
	queryAll := func(ctx context.Context) ([]*model.Session, error) {
		return r.Query().All(ctx)
	}
	countAll := func(ctx context.Context) (int, error) {
		return r.Query().Count(ctx)
	}
	cache, err := getOrCreateCache[model.Session](ctx, idFunc, queryAll, countAll)
	if err != nil {
		return nil, false, err
	}
	var refreshFuncs *eagerRefreshFuncs[model.Session]
	if cache != nil && cache.isEager() {
		refreshFuncs = &eagerRefreshFuncs[model.Session]{cacheID: internal.GetCacheKey[model.Session](ctx), queryAll: queryAll, countAll: countAll, idFunc: idFunc}
	}
	return r.readWithCache(ctx, id, rid, cache, refreshFuncs)
}

// Update updates the record for the given model.
// Before- and after-update hooks are invoked.
func (r *session) Update(ctx context.Context, session *model.Session) error {
	if session == nil {
		return errors.New("the passed node must not be nil")
	}
	if session.ID() == "" {
		return errors.New("cannot update Session without existing record ID")
	}
	if err := r.runHooks(ctx, beforeUpdate, session); err != nil {
		return err
	}
	if err := r.update(ctx, r.recordID(string(session.ID())), session); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterUpdate, session); err != nil {
		return err
	}
	return nil
}

// Delete deletes the record for the given model.
// Before- and after-delete hooks are invoked.
func (r *session) Delete(ctx context.Context, session *model.Session) error {
	if session == nil {
		return errors.New("the passed node must not be nil")
	}
	if session.ID() == "" {
		return errors.New("cannot delete Session without existing record ID")
	}
	if err := r.runHooks(ctx, beforeDelete, session); err != nil {
		return err
	}
	if err := r.delete(ctx, r.recordID(string(session.ID())), session, false, nil); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterDelete, session); err != nil {
		return err
	}
	return nil
}

// Refresh refreshes the given model with the remote data.
func (r *session) Refresh(ctx context.Context, session *model.Session) error {
	if session == nil {
		return errors.New("the passed node must not be nil")
	}
	if session.ID() == "" {
		return errors.New("cannot refresh Session without existing record ID")
	}
	return r.refresh(ctx, r.recordID(string(session.ID())), session)
}

// Relate returns a new relate instance for the Session model.
func (r *session) Relate() *relate.Session {
	return relate.NewSession(r.db)
}

// Index returns a new index instance for the Session model.
func (r *session) Index() *index.Session {
	return index.NewSession(r.db)
}
