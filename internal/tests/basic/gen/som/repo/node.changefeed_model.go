// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package repo

import (
	"context"
	"errors"
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	som "som.test/gen/som"
	conv "som.test/gen/som/conv"
	index "som.test/gen/som/index"
	internal "som.test/gen/som/internal"
	query "som.test/gen/som/query"
	relate "som.test/gen/som/relate"
	model "som.test/model"
)

type ChangefeedModelRepo interface {
	// Query returns a new query builder for the ChangefeedModel model.

	Query() query.Builder[model.ChangefeedModel]
	// Create creates a new record for the ChangefeedModel model.

	Create(ctx context.Context, changefeedModel *model.ChangefeedModel) error
	// Insert creates multiple records in a single operation.
	// Before- and after-create hooks are invoked for each node.

	Insert(ctx context.Context, nodes []*model.ChangefeedModel) error
	// CreateWithID creates a new record with the given ID for the ChangefeedModel model.

	CreateWithID(ctx context.Context, id string, changefeedModel *model.ChangefeedModel) error
	// Read returns the record for the given ID, if it exists.

	Read(ctx context.Context, id string) (*model.ChangefeedModel, bool, error)
	// Update updates the record for the given ChangefeedModel model.

	Update(ctx context.Context, changefeedModel *model.ChangefeedModel) error
	// Delete deletes the record for the given ChangefeedModel model.

	Delete(ctx context.Context, changefeedModel *model.ChangefeedModel) error
	// Refresh refreshes the given model with the current database state.

	Refresh(ctx context.Context, changefeedModel *model.ChangefeedModel) error
	// Relate returns a new relate builder for the ChangefeedModel model.

	Relate() *relate.ChangefeedModel
	// Index returns a new index instance for the ChangefeedModel model.

	Index() *index.ChangefeedModel
	// Changes returns a new changes query builder for the ChangefeedModel model.
	// This is only available for models with changefeed enabled.

	Changes() query.ChangesBuilder[model.ChangefeedModel, conv.ChangefeedModel]

	// OnBeforeCreate registers a hook that runs before a record is created.
	// If the hook returns an error, the create operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeCreate(fn func(ctx context.Context, node *model.ChangefeedModel) error) func()
	// OnAfterCreate registers a hook that runs after a record has been created.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterCreate(fn func(ctx context.Context, node *model.ChangefeedModel) error) func()
	// OnBeforeUpdate registers a hook that runs before a record is updated.
	// If the hook returns an error, the update operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeUpdate(fn func(ctx context.Context, node *model.ChangefeedModel) error) func()
	// OnAfterUpdate registers a hook that runs after a record has been updated.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterUpdate(fn func(ctx context.Context, node *model.ChangefeedModel) error) func()
	// OnBeforeDelete registers a hook that runs before a record is deleted.
	// If the hook returns an error, the delete operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeDelete(fn func(ctx context.Context, node *model.ChangefeedModel) error) func()
	// OnAfterDelete registers a hook that runs after a record has been deleted.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterDelete(fn func(ctx context.Context, node *model.ChangefeedModel) error) func()
}

// changefeedModelRepoInfo holds the model-specific conversion functions for ChangefeedModel.
var changefeedModelRepoInfo = RepoInfo[model.ChangefeedModel]{
	CreateNew: func(ctx context.Context, db *dbConn, idExpr string, data any) (*model.ChangefeedModel, error) {
		raw, err := dbCreateNew[conv.ChangefeedModel](ctx, db, idExpr, data)
		if err != nil {
			return nil, err
		}
		return conv.ToChangefeedModelPtr(raw), nil
	},
	CreateOne: func(ctx context.Context, db *dbConn, id models.RecordID, data any) (*model.ChangefeedModel, error) {
		raw, err := dbCreate[conv.ChangefeedModel](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToChangefeedModelPtr(raw), nil
	},
	InsertAll: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) ([]*model.ChangefeedModel, error) {
		raw, err := dbInsert[conv.ChangefeedModel](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		results := make([]*model.ChangefeedModel, len(raw))
		for i, r := range raw {
			results[i] = conv.ToChangefeedModelPtr(r)
		}
		return results, nil
	},
	MarshalOne: func(node *model.ChangefeedModel) any {
		return conv.FromChangefeedModelPtr(node)
	},
	QueryOne: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) (*model.ChangefeedModel, error) {
		raw, err := dbQueryOne[conv.ChangefeedModel](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToChangefeedModelPtr(raw), nil
	},
	ReadOne: func(ctx context.Context, db *dbConn, id *models.RecordID) (*model.ChangefeedModel, error) {
		raw, err := dbSelect[conv.ChangefeedModel](ctx, db, id)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToChangefeedModelPtr(raw), nil
	},
	UpdateOne: func(ctx context.Context, db *dbConn, id *models.RecordID, data any) (*model.ChangefeedModel, error) {
		raw, err := dbUpdate[conv.ChangefeedModel](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToChangefeedModelPtr(raw), nil
	},
}

// ChangefeedModelRepo returns the repository instance for the ChangefeedModel model.
// The instance is cached as a singleton on the client.
func (c *ClientImpl) ChangefeedModelRepo() ChangefeedModelRepo {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.changefeedModelRepo == nil {
		c.changefeedModelRepo = &changefeedModel{repo: &repo[model.ChangefeedModel, string]{
			db:     c.db,
			name:   "changefeed_model",
			info:   changefeedModelRepoInfo,
			newID:  newULID,
			idFunc: "rand::ulid()",
			recordID: func(id string) *models.RecordID {
				rid := models.NewRecordID("changefeed_model", parseStringID(id))
				return &rid
			}}}
	}
	return c.changefeedModelRepo
}

type changefeedModel struct {
	*repo[model.ChangefeedModel, string]
}

// Query returns a new query builder for the ChangefeedModel model.
func (r *changefeedModel) Query() query.Builder[model.ChangefeedModel] {
	return query.NewChangefeedModel(r.db)
}

// Create creates a new record for the ChangefeedModel model.
// The ID will be generated automatically as a ULID.
// Before- and after-create hooks are invoked.
func (r *changefeedModel) Create(ctx context.Context, changefeedModel *model.ChangefeedModel) error {
	if changefeedModel == nil {
		return errors.New("the passed node must not be nil")
	}
	if changefeedModel.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, changefeedModel); err != nil {
		return err
	}
	if err := r.create(ctx, changefeedModel); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, changefeedModel); err != nil {
		return err
	}
	return nil
}

// CreateWithID creates a new record for the ChangefeedModel model with the given id.
// Before- and after-create hooks are invoked.
func (r *changefeedModel) CreateWithID(ctx context.Context, id string, changefeedModel *model.ChangefeedModel) error {
	if changefeedModel == nil {
		return errors.New("the passed node must not be nil")
	}
	if id == "" {
		return som.ErrEmptyID
	}
	if changefeedModel.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, changefeedModel); err != nil {
		return err
	}
	if err := r.createWithID(ctx, id, changefeedModel); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, changefeedModel); err != nil {
		return err
	}
	return nil
}

// Insert creates multiple records in a single operation.
// Before- and after-create hooks are invoked for each node.
func (r *changefeedModel) Insert(ctx context.Context, nodes []*model.ChangefeedModel) error {
	if len(nodes) == 0 {
		return nil
	}
	for _, n := range nodes {
		if n == nil {
			return errors.New("slice contains nil node")
		}
		if n.ID() != "" {
			return errors.New("node already has an id")
		}
	}
	if err := r.runHooksAll(ctx, beforeCreate, nodes); err != nil {
		return err
	}
	if err := r.insert(ctx, nodes); err != nil {
		return err
	}
	if err := r.runHooksAll(ctx, afterCreate, nodes); err != nil {
		return err
	}
	return nil
}

// Read returns the record for the given id, if it exists.
// The returned bool indicates whether the record was found or not.
// If caching is enabled via som.WithCache, it will be used.
func (r *changefeedModel) Read(ctx context.Context, id string) (*model.ChangefeedModel, bool, error) {
	if id == "" {
		return nil, false, som.ErrEmptyID
	}
	rid := r.recordID(id)
	if internal.TxActive(ctx) {
		return r.read(ctx, rid)
	}
	if !internal.CacheEnabled[model.ChangefeedModel](ctx) {
		return r.read(ctx, rid)
	}
	idFunc := func(n *model.ChangefeedModel) string {
		return string(n.ID())
	}
	queryAll := func(ctx context.Context) ([]*model.ChangefeedModel, error) {
		return r.Query().All(ctx)
	}
	countAll := func(ctx context.Context) (int, error) {
		return r.Query().Count(ctx)
	}
	cache, err := getOrCreateCache[model.ChangefeedModel](ctx, idFunc, queryAll, countAll)
	if err != nil {
		return nil, false, err
	}
	var refreshFuncs *eagerRefreshFuncs[model.ChangefeedModel]
	if cache != nil && cache.isEager() {
		refreshFuncs = &eagerRefreshFuncs[model.ChangefeedModel]{cacheID: internal.GetCacheKey[model.ChangefeedModel](ctx), queryAll: queryAll, countAll: countAll, idFunc: idFunc}
	}
	return r.readWithCache(ctx, id, rid, cache, refreshFuncs)
}

// Update updates the record for the given model.
// Before- and after-update hooks are invoked.
func (r *changefeedModel) Update(ctx context.Context, changefeedModel *model.ChangefeedModel) error {
	if changefeedModel == nil {
		return errors.New("the passed node must not be nil")
	}
	if changefeedModel.ID() == "" {
		return errors.New("cannot update ChangefeedModel without existing record ID")
	}
	if err := r.runHooks(ctx, beforeUpdate, changefeedModel); err != nil {
		return err
	}
	if err := r.update(ctx, r.recordID(string(changefeedModel.ID())), changefeedModel); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterUpdate, changefeedModel); err != nil {
		return err
	}
	return nil
}

// Delete deletes the record for the given model.
// Before- and after-delete hooks are invoked.
func (r *changefeedModel) Delete(ctx context.Context, changefeedModel *model.ChangefeedModel) error {
	if changefeedModel == nil {
		return errors.New("the passed node must not be nil")
	}
	if changefeedModel.ID() == "" {
		return errors.New("cannot delete ChangefeedModel without existing record ID")
	}
	if err := r.runHooks(ctx, beforeDelete, changefeedModel); err != nil {
		return err
	}
	if err := r.delete(ctx, r.recordID(string(changefeedModel.ID())), changefeedModel, false, nil); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterDelete, changefeedModel); err != nil {
		return err
	}
	return nil
}

// Refresh refreshes the given model with the remote data.
func (r *changefeedModel) Refresh(ctx context.Context, changefeedModel *model.ChangefeedModel) error {
	if changefeedModel == nil {
		return errors.New("the passed node must not be nil")
	}
	if changefeedModel.ID() == "" {
		return errors.New("cannot refresh ChangefeedModel without existing record ID")
	}
	return r.refresh(ctx, r.recordID(string(changefeedModel.ID())), changefeedModel)
}

// Relate returns a new relate instance for the ChangefeedModel model.
func (r *changefeedModel) Relate() *relate.ChangefeedModel {
	return relate.NewChangefeedModel(r.db)
}

// Index returns a new index instance for the ChangefeedModel model.
func (r *changefeedModel) Index() *index.ChangefeedModel {
	return index.NewChangefeedModel(r.db)
}

// Changes returns a new changes query builder for the ChangefeedModel model.
// This is only available for models with changefeed enabled.
func (r *changefeedModel) Changes() query.ChangesBuilder[model.ChangefeedModel, conv.ChangefeedModel] {
	return query.NewChangefeedModelChanges(r.db)
}
