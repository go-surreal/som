// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package repo

import (
	"context"
	"errors"
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	som "som.test/gen/som"
	conv "som.test/gen/som/conv"
	index "som.test/gen/som/index"
	internal "som.test/gen/som/internal"
	query "som.test/gen/som/query"
	relate "som.test/gen/som/relate"
	model "som.test/model"
)

type EphemeralRepo interface {
	// Query returns a new query builder for the Ephemeral model.

	Query() query.Builder[model.Ephemeral]
	// Create creates a new record for the Ephemeral model.

	Create(ctx context.Context, ephemeral *model.Ephemeral) error
	// Insert creates multiple records in a single operation.
	// Before- and after-create hooks are invoked for each node.

	Insert(ctx context.Context, nodes []*model.Ephemeral) error
	// CreateWithID creates a new record with the given ID for the Ephemeral model.

	CreateWithID(ctx context.Context, id string, ephemeral *model.Ephemeral) error
	// Read returns the record for the given ID, if it exists.

	Read(ctx context.Context, id string) (*model.Ephemeral, bool, error)
	// Update updates the record for the given Ephemeral model.

	Update(ctx context.Context, ephemeral *model.Ephemeral) error
	// Delete deletes the record for the given Ephemeral model.

	Delete(ctx context.Context, ephemeral *model.Ephemeral) error
	// Refresh refreshes the given model with the current database state.

	Refresh(ctx context.Context, ephemeral *model.Ephemeral) error
	// Relate returns a new relate builder for the Ephemeral model.

	Relate() *relate.Ephemeral
	// Index returns a new index instance for the Ephemeral model.

	Index() *index.Ephemeral

	// OnBeforeCreate registers a hook that runs before a record is created.
	// If the hook returns an error, the create operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeCreate(fn func(ctx context.Context, node *model.Ephemeral) error) func()
	// OnAfterCreate registers a hook that runs after a record has been created.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterCreate(fn func(ctx context.Context, node *model.Ephemeral) error) func()
	// OnBeforeUpdate registers a hook that runs before a record is updated.
	// If the hook returns an error, the update operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeUpdate(fn func(ctx context.Context, node *model.Ephemeral) error) func()
	// OnAfterUpdate registers a hook that runs after a record has been updated.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterUpdate(fn func(ctx context.Context, node *model.Ephemeral) error) func()
	// OnBeforeDelete registers a hook that runs before a record is deleted.
	// If the hook returns an error, the delete operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeDelete(fn func(ctx context.Context, node *model.Ephemeral) error) func()
	// OnAfterDelete registers a hook that runs after a record has been deleted.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterDelete(fn func(ctx context.Context, node *model.Ephemeral) error) func()
}

// ephemeralRepoInfo holds the model-specific conversion functions for Ephemeral.
var ephemeralRepoInfo = RepoInfo[model.Ephemeral]{
	CreateNew: func(ctx context.Context, db *dbConn, target string, data any) (*model.Ephemeral, error) {
		raw, err := dbCreateNew[conv.Ephemeral](ctx, db, target, data)
		if err != nil {
			return nil, err
		}
		return conv.ToEphemeralPtr(raw), nil
	},
	CreateOne: func(ctx context.Context, db *dbConn, id models.RecordID, data any) (*model.Ephemeral, error) {
		raw, err := dbCreate[conv.Ephemeral](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToEphemeralPtr(raw), nil
	},
	InsertAll: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) ([]*model.Ephemeral, error) {
		raw, err := dbInsert[conv.Ephemeral](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		results := make([]*model.Ephemeral, len(raw))
		for i, r := range raw {
			results[i] = conv.ToEphemeralPtr(r)
		}
		return results, nil
	},
	MarshalOne: func(node *model.Ephemeral) any {
		return conv.FromEphemeralPtr(node)
	},
	QueryOne: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) (*model.Ephemeral, error) {
		raw, err := dbQueryOne[conv.Ephemeral](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToEphemeralPtr(raw), nil
	},
	ReadOne: func(ctx context.Context, db *dbConn, id *models.RecordID) (*model.Ephemeral, error) {
		raw, err := dbSelect[conv.Ephemeral](ctx, db, id)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToEphemeralPtr(raw), nil
	},
	UpdateOne: func(ctx context.Context, db *dbConn, id *models.RecordID, data any) (*model.Ephemeral, error) {
		raw, err := dbUpdate[conv.Ephemeral](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToEphemeralPtr(raw), nil
	},
}

// EphemeralRepo returns the repository instance for the Ephemeral model.
// The instance is cached as a singleton on the client.
func (c *ClientImpl) EphemeralRepo() EphemeralRepo {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.ephemeralRepo == nil {
		c.ephemeralRepo = &ephemeral{repo: &repo[model.Ephemeral, string]{
			db:     c.db,
			name:   "ephemeral",
			info:   ephemeralRepoInfo,
			autoID: true,
			recordID: func(id string) *models.RecordID {
				rid := models.NewRecordID("ephemeral", parseUUID(id))
				return &rid
			}}}
	}
	return c.ephemeralRepo
}

type ephemeral struct {
	*repo[model.Ephemeral, string]
}

// Query returns a new query builder for the Ephemeral model.
func (r *ephemeral) Query() query.Builder[model.Ephemeral] {
	return query.NewEphemeral(r.db)
}

// Create creates a new record for the Ephemeral model.
// The ID will be generated automatically as a ULID.
// Before- and after-create hooks are invoked.
func (r *ephemeral) Create(ctx context.Context, ephemeral *model.Ephemeral) error {
	if ephemeral == nil {
		return errors.New("the passed node must not be nil")
	}
	if ephemeral.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, ephemeral); err != nil {
		return err
	}
	if err := r.create(ctx, ephemeral); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, ephemeral); err != nil {
		return err
	}
	return nil
}

// CreateWithID creates a new record for the Ephemeral model with the given id.
// Before- and after-create hooks are invoked.
func (r *ephemeral) CreateWithID(ctx context.Context, id string, ephemeral *model.Ephemeral) error {
	if ephemeral == nil {
		return errors.New("the passed node must not be nil")
	}
	if id == "" {
		return som.ErrEmptyID
	}
	if ephemeral.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, ephemeral); err != nil {
		return err
	}
	if err := r.createWithID(ctx, id, ephemeral); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, ephemeral); err != nil {
		return err
	}
	return nil
}

// Insert creates multiple records in a single operation.
// Before- and after-create hooks are invoked for each node.
func (r *ephemeral) Insert(ctx context.Context, nodes []*model.Ephemeral) error {
	if len(nodes) == 0 {
		return nil
	}
	for _, n := range nodes {
		if n == nil {
			return errors.New("slice contains nil node")
		}
		if n.ID() != "" {
			return errors.New("node already has an id")
		}
	}
	if err := r.runHooksAll(ctx, beforeCreate, nodes); err != nil {
		return err
	}
	if err := r.insert(ctx, nodes); err != nil {
		return err
	}
	if err := r.runHooksAll(ctx, afterCreate, nodes); err != nil {
		return err
	}
	return nil
}

// Read returns the record for the given id, if it exists.
// The returned bool indicates whether the record was found or not.
// If caching is enabled via som.WithCache, it will be used.
func (r *ephemeral) Read(ctx context.Context, id string) (*model.Ephemeral, bool, error) {
	if id == "" {
		return nil, false, som.ErrEmptyID
	}
	rid := r.recordID(id)
	if internal.TxActive(ctx) {
		return r.read(ctx, rid)
	}
	if !internal.CacheEnabled[model.Ephemeral](ctx) {
		return r.read(ctx, rid)
	}
	idFunc := func(n *model.Ephemeral) string {
		return string(n.ID())
	}
	queryAll := func(ctx context.Context) ([]*model.Ephemeral, error) {
		return r.Query().All(ctx)
	}
	countAll := func(ctx context.Context) (int, error) {
		return r.Query().Count(ctx)
	}
	cache, err := getOrCreateCache[model.Ephemeral](ctx, idFunc, queryAll, countAll)
	if err != nil {
		return nil, false, err
	}
	var refreshFuncs *eagerRefreshFuncs[model.Ephemeral]
	if cache != nil && cache.isEager() {
		refreshFuncs = &eagerRefreshFuncs[model.Ephemeral]{cacheID: internal.GetCacheKey[model.Ephemeral](ctx), queryAll: queryAll, countAll: countAll, idFunc: idFunc}
	}
	return r.readWithCache(ctx, id, rid, cache, refreshFuncs)
}

// Update updates the record for the given model.
// Before- and after-update hooks are invoked.
func (r *ephemeral) Update(ctx context.Context, ephemeral *model.Ephemeral) error {
	if ephemeral == nil {
		return errors.New("the passed node must not be nil")
	}
	if ephemeral.ID() == "" {
		return errors.New("cannot update Ephemeral without existing record ID")
	}
	if err := r.runHooks(ctx, beforeUpdate, ephemeral); err != nil {
		return err
	}
	if err := r.update(ctx, r.recordID(string(ephemeral.ID())), ephemeral); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterUpdate, ephemeral); err != nil {
		return err
	}
	return nil
}

// Delete deletes the record for the given model.
// Before- and after-delete hooks are invoked.
func (r *ephemeral) Delete(ctx context.Context, ephemeral *model.Ephemeral) error {
	if ephemeral == nil {
		return errors.New("the passed node must not be nil")
	}
	if ephemeral.ID() == "" {
		return errors.New("cannot delete Ephemeral without existing record ID")
	}
	if err := r.runHooks(ctx, beforeDelete, ephemeral); err != nil {
		return err
	}
	if err := r.delete(ctx, r.recordID(string(ephemeral.ID())), ephemeral, false, nil); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterDelete, ephemeral); err != nil {
		return err
	}
	return nil
}

// Refresh refreshes the given model with the remote data.
func (r *ephemeral) Refresh(ctx context.Context, ephemeral *model.Ephemeral) error {
	if ephemeral == nil {
		return errors.New("the passed node must not be nil")
	}
	if ephemeral.ID() == "" {
		return errors.New("cannot refresh Ephemeral without existing record ID")
	}
	return r.refresh(ctx, r.recordID(string(ephemeral.ID())), ephemeral)
}

// Relate returns a new relate instance for the Ephemeral model.
func (r *ephemeral) Relate() *relate.Ephemeral {
	return relate.NewEphemeral(r.db)
}

// Index returns a new index instance for the Ephemeral model.
func (r *ephemeral) Index() *index.Ephemeral {
	return index.NewEphemeral(r.db)
}
