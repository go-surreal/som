// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package repo

import (
	"context"
	"errors"
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	som "som.test/gen/som"
	conv "som.test/gen/som/conv"
	index "som.test/gen/som/index"
	internal "som.test/gen/som/internal"
	query "som.test/gen/som/query"
	relate "som.test/gen/som/relate"
	model "som.test/model"
)

type SlugRepo interface {
	// Query returns a new query builder for the Slug model.

	Query() query.Builder[model.Slug]
	// Insert creates multiple records in a single operation.
	// Every node must have a non-empty ID set.
	// Before- and after-create hooks are invoked for each node.

	Insert(ctx context.Context, nodes []*model.Slug) error
	// CreateWithID creates a new record with the given ID for the Slug model.

	CreateWithID(ctx context.Context, id string, slug *model.Slug) error
	// Read returns the record for the given ID, if it exists.

	Read(ctx context.Context, id string) (*model.Slug, bool, error)
	// Update updates the record for the given Slug model.

	Update(ctx context.Context, slug *model.Slug) error
	// Delete deletes the record for the given Slug model.

	Delete(ctx context.Context, slug *model.Slug) error
	// Refresh refreshes the given model with the current database state.

	Refresh(ctx context.Context, slug *model.Slug) error
	// Relate returns a new relate builder for the Slug model.

	Relate() *relate.Slug
	// Index returns a new index instance for the Slug model.

	Index() *index.Slug

	// OnBeforeCreate registers a hook that runs before a record is created.
	// If the hook returns an error, the create operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeCreate(fn func(ctx context.Context, node *model.Slug) error) func()
	// OnAfterCreate registers a hook that runs after a record has been created.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterCreate(fn func(ctx context.Context, node *model.Slug) error) func()
	// OnBeforeUpdate registers a hook that runs before a record is updated.
	// If the hook returns an error, the update operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeUpdate(fn func(ctx context.Context, node *model.Slug) error) func()
	// OnAfterUpdate registers a hook that runs after a record has been updated.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterUpdate(fn func(ctx context.Context, node *model.Slug) error) func()
	// OnBeforeDelete registers a hook that runs before a record is deleted.
	// If the hook returns an error, the delete operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnBeforeDelete(fn func(ctx context.Context, node *model.Slug) error) func()
	// OnAfterDelete registers a hook that runs after a record has been deleted.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.

	OnAfterDelete(fn func(ctx context.Context, node *model.Slug) error) func()
}

// slugRepoInfo holds the model-specific conversion functions for Slug.
var slugRepoInfo = RepoInfo[model.Slug]{
	CreateNew: func(ctx context.Context, db *dbConn, target string, data any) (*model.Slug, error) {
		raw, err := dbCreateNew[conv.Slug](ctx, db, target, data)
		if err != nil {
			return nil, err
		}
		return conv.ToSlugPtr(raw), nil
	},
	CreateOne: func(ctx context.Context, db *dbConn, id models.RecordID, data any) (*model.Slug, error) {
		raw, err := dbCreate[conv.Slug](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToSlugPtr(raw), nil
	},
	InsertAll: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) ([]*model.Slug, error) {
		raw, err := dbInsert[conv.Slug](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		results := make([]*model.Slug, len(raw))
		for i, r := range raw {
			results[i] = conv.ToSlugPtr(r)
		}
		return results, nil
	},
	MarshalOne: func(node *model.Slug) any {
		return conv.FromSlugPtr(node)
	},
	QueryOne: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) (*model.Slug, error) {
		raw, err := dbQueryOne[conv.Slug](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToSlugPtr(raw), nil
	},
	ReadOne: func(ctx context.Context, db *dbConn, id *models.RecordID) (*model.Slug, error) {
		raw, err := dbSelect[conv.Slug](ctx, db, id)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToSlugPtr(raw), nil
	},
	UpdateOne: func(ctx context.Context, db *dbConn, id *models.RecordID, data any) (*model.Slug, error) {
		raw, err := dbUpdate[conv.Slug](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToSlugPtr(raw), nil
	},
}

// SlugRepo returns the repository instance for the Slug model.
// The instance is cached as a singleton on the client.
func (c *ClientImpl) SlugRepo() SlugRepo {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.slugRepo == nil {
		c.slugRepo = &slug{repo: &repo[model.Slug, string]{
			db:   c.db,
			name: "slug",
			info: slugRepoInfo,
			recordID: func(id string) *models.RecordID {
				rid := models.NewRecordID("slug", parseStringID(id))
				return &rid
			}}}
	}
	return c.slugRepo
}

type slug struct {
	*repo[model.Slug, string]
}

// Query returns a new query builder for the Slug model.
func (r *slug) Query() query.Builder[model.Slug] {
	return query.NewSlug(r.db)
}

// CreateWithID creates a new record for the Slug model with the given id.
// Before- and after-create hooks are invoked.
func (r *slug) CreateWithID(ctx context.Context, id string, slug *model.Slug) error {
	if slug == nil {
		return errors.New("the passed node must not be nil")
	}
	if id == "" {
		return som.ErrEmptyID
	}
	if slug.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, slug); err != nil {
		return err
	}
	if err := r.createWithID(ctx, id, slug); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, slug); err != nil {
		return err
	}
	return nil
}

// Insert creates multiple records in a single operation.
// Every node must have a non-empty ID set.
// Before- and after-create hooks are invoked for each node.
func (r *slug) Insert(ctx context.Context, nodes []*model.Slug) error {
	if len(nodes) == 0 {
		return nil
	}
	for _, n := range nodes {
		if n == nil {
			return errors.New("slice contains nil node")
		}
		if n.ID() == "" {
			return som.ErrEmptyID
		}
	}
	if err := r.runHooksAll(ctx, beforeCreate, nodes); err != nil {
		return err
	}
	if err := r.insert(ctx, nodes); err != nil {
		return err
	}
	if err := r.runHooksAll(ctx, afterCreate, nodes); err != nil {
		return err
	}
	return nil
}

// Read returns the record for the given id, if it exists.
// The returned bool indicates whether the record was found or not.
// If caching is enabled via som.WithCache, it will be used.
func (r *slug) Read(ctx context.Context, id string) (*model.Slug, bool, error) {
	if id == "" {
		return nil, false, som.ErrEmptyID
	}
	rid := r.recordID(id)
	if internal.TxActive(ctx) {
		return r.read(ctx, rid)
	}
	if !internal.CacheEnabled[model.Slug](ctx) {
		return r.read(ctx, rid)
	}
	idFunc := func(n *model.Slug) string {
		return string(n.ID())
	}
	queryAll := func(ctx context.Context) ([]*model.Slug, error) {
		return r.Query().All(ctx)
	}
	countAll := func(ctx context.Context) (int, error) {
		return r.Query().Count(ctx)
	}
	cache, err := getOrCreateCache[model.Slug](ctx, idFunc, queryAll, countAll)
	if err != nil {
		return nil, false, err
	}
	var refreshFuncs *eagerRefreshFuncs[model.Slug]
	if cache != nil && cache.isEager() {
		refreshFuncs = &eagerRefreshFuncs[model.Slug]{cacheID: internal.GetCacheKey[model.Slug](ctx), queryAll: queryAll, countAll: countAll, idFunc: idFunc}
	}
	return r.readWithCache(ctx, id, rid, cache, refreshFuncs)
}

// Update updates the record for the given model.
// Before- and after-update hooks are invoked.
func (r *slug) Update(ctx context.Context, slug *model.Slug) error {
	if slug == nil {
		return errors.New("the passed node must not be nil")
	}
	if slug.ID() == "" {
		return errors.New("cannot update Slug without existing record ID")
	}
	if err := r.runHooks(ctx, beforeUpdate, slug); err != nil {
		return err
	}
	if err := r.update(ctx, r.recordID(string(slug.ID())), slug); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterUpdate, slug); err != nil {
		return err
	}
	return nil
}

// Delete deletes the record for the given model.
// Before- and after-delete hooks are invoked.
func (r *slug) Delete(ctx context.Context, slug *model.Slug) error {
	if slug == nil {
		return errors.New("the passed node must not be nil")
	}
	if slug.ID() == "" {
		return errors.New("cannot delete Slug without existing record ID")
	}
	if err := r.runHooks(ctx, beforeDelete, slug); err != nil {
		return err
	}
	if err := r.delete(ctx, r.recordID(string(slug.ID())), slug, false, nil); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterDelete, slug); err != nil {
		return err
	}
	return nil
}

// Refresh refreshes the given model with the remote data.
func (r *slug) Refresh(ctx context.Context, slug *model.Slug) error {
	if slug == nil {
		return errors.New("the passed node must not be nil")
	}
	if slug.ID() == "" {
		return errors.New("cannot refresh Slug without existing record ID")
	}
	return r.refresh(ctx, r.recordID(string(slug.ID())), slug)
}

// Relate returns a new relate instance for the Slug model.
func (r *slug) Relate() *relate.Slug {
	return relate.NewSlug(r.db)
}

// Index returns a new index instance for the Slug model.
func (r *slug) Index() *index.Slug {
	return index.NewSlug(r.db)
}
