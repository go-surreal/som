// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package repo

import (
	"context"
	"errors"
	"fmt"
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	"slices"
	som "som.test/gen/som"
	conv "som.test/gen/som/conv"
	index "som.test/gen/som/index"
	internal "som.test/gen/som/internal"
	query "som.test/gen/som/query"
	relate "som.test/gen/som/relate"
	with "som.test/gen/som/with"
	model "som.test/model"
)

type SquareRepo interface {
	// Query returns a new query builder for the Square model.
	Query() query.Builder[model.Square]
	// Create creates a new record for the Square model.
	Create(ctx context.Context, square *model.Square) error
	// Insert creates multiple records in a single operation.
	// Before- and after-create hooks are invoked for each node.
	Insert(ctx context.Context, nodes []*model.Square) error
	// CreateWithID creates a new record with the given ID for the Square model.
	CreateWithID(ctx context.Context, id string, square *model.Square) error
	// Read returns the record for the given ID, if it exists.
	Read(ctx context.Context, id string) (*model.Square, bool, error)
	// Update updates the record for the given Square model.
	Update(ctx context.Context, square *model.Square) error
	// Delete deletes the record for the given Square model.
	Delete(ctx context.Context, square *model.Square) error
	// Refresh refreshes the given model with the current database state.
	Refresh(ctx context.Context, square *model.Square) error
	// Relate returns a new relate builder for the Square model.
	Relate() *relate.Square
	// Resolve loads the given relations of the model from the database.
	Resolve(ctx context.Context, square *model.Square, fetch ...with.Fetch_[model.Square]) error
	// Index returns a new index instance for the Square model.
	Index() *index.Square

	// OnBeforeCreate registers a hook that runs before a record is created.
	// If the hook returns an error, the create operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeCreate(fn func(ctx context.Context, node *model.Square) error) func()
	// OnAfterCreate registers a hook that runs after a record has been created.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterCreate(fn func(ctx context.Context, node *model.Square) error) func()
	// OnBeforeUpdate registers a hook that runs before a record is updated.
	// If the hook returns an error, the update operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeUpdate(fn func(ctx context.Context, node *model.Square) error) func()
	// OnAfterUpdate registers a hook that runs after a record has been updated.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterUpdate(fn func(ctx context.Context, node *model.Square) error) func()
	// OnBeforeDelete registers a hook that runs before a record is deleted.
	// If the hook returns an error, the delete operation is aborted.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnBeforeDelete(fn func(ctx context.Context, node *model.Square) error) func()
	// OnAfterDelete registers a hook that runs after a record has been deleted.
	// If the hook returns an error, the error is returned to the caller.
	// Returns a function that, when called, removes this hook.
	//
	// Note: Hooks are local to this application instance and are not
	// distributed across multiple instances of the application.
	OnAfterDelete(fn func(ctx context.Context, node *model.Square) error) func()
}

// squareRepoInfo holds the model-specific conversion functions for Square.
var squareRepoInfo = RepoInfo[model.Square]{
	CreateNew: func(ctx context.Context, db *dbConn, target string, data any) (*model.Square, error) {
		raw, err := dbCreateNew[conv.Square](ctx, db, target, data)
		if err != nil {
			return nil, err
		}
		return conv.ToSquarePtr(raw), nil
	},
	CreateOne: func(ctx context.Context, db *dbConn, id models.RecordID, data any) (*model.Square, error) {
		raw, err := dbCreate[conv.Square](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToSquarePtr(raw), nil
	},
	InsertAll: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) ([]*model.Square, error) {
		raw, err := dbInsert[conv.Square](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		results := make([]*model.Square, len(raw))
		for i, r := range raw {
			results[i] = conv.ToSquarePtr(r)
		}
		return results, nil
	},
	MarshalOne: func(node *model.Square) any {
		return conv.FromSquarePtr(node)
	},
	MergeOne: func(node *model.Square, data []byte) error {
		into := conv.FromSquarePtr(node)
		if err := into.UnmarshalCBOR(data); err != nil {
			return err
		}
		*node = *conv.ToSquarePtr(into)
		return nil
	},
	QueryOne: func(ctx context.Context, db *dbConn, stmt string, vars map[string]any) (*model.Square, error) {
		raw, err := dbQueryOne[conv.Square](ctx, db, stmt, vars)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToSquarePtr(raw), nil
	},
	ReadOne: func(ctx context.Context, db *dbConn, id *models.RecordID) (*model.Square, error) {
		raw, err := dbSelect[conv.Square](ctx, db, id)
		if err != nil {
			return nil, err
		}
		if raw == nil {
			return nil, nil
		}
		return conv.ToSquarePtr(raw), nil
	},
	UpdateOne: func(ctx context.Context, db *dbConn, id *models.RecordID, data any) (*model.Square, error) {
		raw, err := dbUpdate[conv.Square](ctx, db, id, data)
		if err != nil {
			return nil, err
		}
		return conv.ToSquarePtr(raw), nil
	},
}

// SquareRepo returns the repository instance for the Square model.
// The instance is cached as a singleton on the client.
func (c *ClientImpl) SquareRepo() SquareRepo {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.squareRepo == nil {
		c.squareRepo = &square{repo: &repo[model.Square, string]{
			db:     c.db,
			name:   "square",
			info:   squareRepoInfo,
			autoID: true,
			recordID: func(id string) *models.RecordID {
				rid := models.NewRecordID("square", parseStringID(id))
				return &rid
			},
		}}
	}
	return c.squareRepo
}

type square struct {
	*repo[model.Square, string]
}

// Query returns a new query builder for the Square model.
func (r *square) Query() query.Builder[model.Square] {
	return query.NewSquare(r.db)
}

// Create creates a new record for the Square model.
// The ID will be generated automatically as a ULID.
// Before- and after-create hooks are invoked.
func (r *square) Create(ctx context.Context, square *model.Square) error {
	if square == nil {
		return errors.New("the passed node must not be nil")
	}
	if square.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, square); err != nil {
		return err
	}
	if err := r.create(ctx, square); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, square); err != nil {
		return err
	}
	return nil
}

// CreateWithID creates a new record for the Square model with the given id.
// Before- and after-create hooks are invoked.
func (r *square) CreateWithID(ctx context.Context, id string, square *model.Square) error {
	if square == nil {
		return errors.New("the passed node must not be nil")
	}
	if id == "" {
		return som.ErrEmptyID
	}
	if square.ID() != "" {
		return errors.New("given node already has an id")
	}
	if err := r.runHooks(ctx, beforeCreate, square); err != nil {
		return err
	}
	if err := r.createWithID(ctx, id, square); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterCreate, square); err != nil {
		return err
	}
	return nil
}

// Insert creates multiple records in a single operation.
// Before- and after-create hooks are invoked for each node.
func (r *square) Insert(ctx context.Context, nodes []*model.Square) error {
	if len(nodes) == 0 {
		return nil
	}
	for _, n := range nodes {
		if n == nil {
			return errors.New("slice contains nil node")
		}
		if n.ID() != "" {
			return errors.New("node already has an id")
		}
	}
	if err := r.runHooksAll(ctx, beforeCreate, nodes); err != nil {
		return err
	}
	if err := r.insert(ctx, nodes); err != nil {
		return err
	}
	if err := r.runHooksAll(ctx, afterCreate, nodes); err != nil {
		return err
	}
	return nil
}

// Read returns the record for the given id, if it exists.
// The returned bool indicates whether the record was found or not.
// If caching is enabled via som.WithCache, it will be used.
func (r *square) Read(ctx context.Context, id string) (*model.Square, bool, error) {
	if id == "" {
		return nil, false, som.ErrEmptyID
	}
	rid := r.recordID(id)
	if internal.TxActive(ctx) {
		return r.read(ctx, rid)
	}
	if !internal.CacheEnabled[model.Square](ctx) {
		return r.read(ctx, rid)
	}
	idFunc := func(n *model.Square) string {
		return string(n.ID())
	}
	queryAll := func(ctx context.Context) ([]*model.Square, error) {
		return r.Query().All(ctx)
	}
	countAll := func(ctx context.Context) (int, error) {
		return r.Query().Count(ctx)
	}
	cache, err := getOrCreateCache[model.Square](ctx, idFunc, queryAll, countAll)
	if err != nil {
		return nil, false, err
	}
	var refreshFuncs *eagerRefreshFuncs[model.Square]
	if cache != nil && cache.isEager() {
		refreshFuncs = &eagerRefreshFuncs[model.Square]{cacheID: internal.GetCacheKey[model.Square](ctx), queryAll: queryAll, countAll: countAll, idFunc: idFunc}
	}
	return r.readWithCache(ctx, id, rid, cache, refreshFuncs)
}

// Update updates the record for the given model.
// Before- and after-update hooks are invoked.
func (r *square) Update(ctx context.Context, square *model.Square) error {
	if square == nil {
		return errors.New("the passed node must not be nil")
	}
	if square.IsPartial() {
		return som.ErrPartialModel
	}
	if square.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if square.ID() == "" {
		return errors.New("cannot update Square without existing record ID")
	}
	if err := r.runHooks(ctx, beforeUpdate, square); err != nil {
		return err
	}
	if err := r.update(ctx, r.recordID(string(square.ID())), square); err != nil {
		return err
	}
	if err := r.runHooks(ctx, afterUpdate, square); err != nil {
		return err
	}
	return nil
}

// Delete deletes the record for the given model.
// Before- and after-delete hooks are invoked.
func (r *square) Delete(ctx context.Context, square *model.Square) error {
	if square == nil {
		return errors.New("the passed node must not be nil")
	}
	if square.IsPartial() {
		return som.ErrPartialModel
	}
	if square.Marker().Has(som.MarkerDeleted) {
		return som.ErrDeletedModel
	}
	if square.ID() == "" {
		return errors.New("cannot delete Square without existing record ID")
	}
	if err := r.runHooks(ctx, beforeDelete, square); err != nil {
		return err
	}
	if err := r.delete(ctx, r.recordID(string(square.ID())), square, false, nil); err != nil {
		return err
	}
	internal.AddMarker(&square.Node, internal.MarkerDeleted)
	if err := r.runHooks(ctx, afterDelete, square); err != nil {
		return err
	}
	return nil
}

// Refresh refreshes the given model with the remote data.
func (r *square) Refresh(ctx context.Context, square *model.Square) error {
	if square == nil {
		return errors.New("the passed node must not be nil")
	}
	if square.ID() == "" {
		return errors.New("cannot refresh Square without existing record ID")
	}
	return r.refresh(ctx, r.recordID(string(square.ID())), square)
}

// Resolve loads the given relations of the model from the database. The model is
// updated in-place, so a resolved relation is no longer marked as partial and
// IsPartial reports whether it was loaded.
//
// Note that a loaded relation is not necessarily up to date. Resolve only makes
// sure the relation holds field values at all, it does not re-read a relation
// that was already loaded. Use Refresh to get current data.
func (r *square) Resolve(ctx context.Context, square *model.Square, fetch ...with.Fetch_[model.Square]) error {
	if square == nil {
		return errors.New("the passed node must not be nil")
	}
	if square.ID() == "" {
		return errors.New("cannot resolve Square without existing record ID")
	}
	// Relations that are already loaded are not requested again.
	var paths []string
	for _, f := range fetch {
		path := fmt.Sprintf("%v", f)
		if path == "" || conv.SquareResolved(square, path) || slices.Contains(paths, path) {
			continue
		}
		paths = append(paths, path)
	}
	return r.resolve(ctx, r.recordID(string(square.ID())), square, paths)
}

// Relate returns a new relate instance for the Square model.
func (r *square) Relate() *relate.Square {
	return relate.NewSquare(r.db)
}

// Index returns a new index instance for the Square model.
func (r *square) Index() *index.Square {
	return index.NewSquare(r.db)
}
