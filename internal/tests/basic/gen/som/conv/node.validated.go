// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package conv

import (
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	som "som.test/gen/som"
	internal "som.test/gen/som/internal"
	cbor "som.test/gen/som/internal/cbor"
	model "som.test/model"
	"strings"
)

type Validated struct {
	model.Validated
}

func (c *Validated) MarshalCBOR() ([]byte, error) {
	if c == nil {
		return cbor.Marshal(nil)
	}
	return cbor.Marshal(c.fields())
}

func (c *Validated) fields() map[string]any {
	data := make(map[string]any, 11)

	// Embedded som.Node/Edge ID field
	if c.ID() != "" {
		data["id"] = models.NewRecordID("validated", c.ID())
	}

	data["name"] = c.Name
	data["contact"] = fromContactAddress(c.Contact)
	if c.ContactPtr != nil {
		data["contact_ptr"] = fromContactAddressPtr(c.ContactPtr)
	}
	if c.Contacts != nil {
		convSlice := make([]contactAddress, len(c.Contacts))
		for i, v := range c.Contacts {
			convSlice[i] = fromContactAddress(v)
		}
		data["contacts"] = convSlice
	}
	data["code"] = c.Code
	if c.CodePtr != nil {
		data["code_ptr"] = c.CodePtr
	}
	if c.Codes != nil {
		data["codes"] = c.Codes
	}
	data["ignored"] = c.Ignored
	if c.Link != nil {
		data["link"] = toValidatedLinkPtr(c.Link)
	}
	if c.Edges != nil {
		data["edges"] = c.Edges
	}

	return data
}

func (c *Validated) UnmarshalCBOR(data []byte) error {
	var rawMap map[string]cbor.RawMessage
	if err := cbor.Unmarshal(data, &rawMap); err != nil {
		return err
	}

	// Embedded som.Node/Edge/View ID field
	if raw, ok := rawMap["id"]; ok {
		var recordID *models.RecordID
		if err := cbor.Unmarshal(raw, &recordID); err != nil {
			return err
		}
		var idStr string
		if recordID != nil {
			s, err := cbor.RecordIDToString(recordID.ID)
			if err != nil {
				return err
			}
			idStr = s
		}
		c.Node = som.NewNode[som.ULID](som.ULID(idStr))
	}

	if raw, ok := rawMap["name"]; ok {
		cbor.Unmarshal(raw, &c.Name)
	}
	if raw, ok := rawMap["contact"]; ok {
		var convVal contactAddress
		cbor.Unmarshal(raw, &convVal)
		c.Contact = toContactAddress(convVal)
	}
	if raw, ok := rawMap["contact_ptr"]; ok {
		var convVal *contactAddress
		cbor.Unmarshal(raw, &convVal)
		c.ContactPtr = toContactAddressPtr(convVal)
	}
	if raw, ok := rawMap["contacts"]; ok {
		var convSlice []contactAddress
		cbor.Unmarshal(raw, &convSlice)
		{
			c.Contacts = make([]model.ContactAddress, len(convSlice))
			for i, v := range convSlice {
				c.Contacts[i] = toContactAddress(v)
			}
		}
	}
	if raw, ok := rawMap["code"]; ok {
		cbor.Unmarshal(raw, &c.Code)
	}
	if raw, ok := rawMap["code_ptr"]; ok {
		cbor.Unmarshal(raw, &c.CodePtr)
	}
	if raw, ok := rawMap["codes"]; ok {
		cbor.Unmarshal(raw, &c.Codes)
	}
	if raw, ok := rawMap["ignored"]; ok {
		cbor.Unmarshal(raw, &c.Ignored)
	}
	if raw, ok := rawMap["link"]; ok {
		var convVal *validatedLink
		cbor.Unmarshal(raw, &convVal)
		c.Link = fromValidatedLinkPtr(convVal)
	}
	if raw, ok := rawMap["edges"]; ok {
		cbor.Unmarshal(raw, &c.Edges)
	}

	// Flag the relations that hold no unresolved links
	internal.AddFetched(&c.Node, validatedFetchedBits(c))

	// Mark the instance as fully loaded from the database
	internal.SetMarker(&c.Node, internal.MarkerLoaded)

	return nil
}

func FromValidated(data model.Validated) Validated {
	return Validated{Validated: data}
}
func FromValidatedPtr(data *model.Validated) *Validated {
	if data == nil {
		return nil
	}
	return &Validated{Validated: *data}
}

func ToValidated(data Validated) model.Validated {
	return data.Validated
}
func ToValidatedPtr(data *Validated) *model.Validated {
	if data == nil {
		return nil
	}
	result := data.Validated
	return &result
}

// ValidatedFields returns the database keyed value map of a model. It is used by
// the query builder to derive pagination cursor values with correct database field
// names and types.
func ValidatedFields(m *model.Validated) map[string]any {
	c := Validated{*m}
	return c.fields()
}

// validatedLink is a Validated as referenced by another record. It marshals
// to its record ID only, but unmarshals from either a record ID or a fetched record.
type validatedLink struct {
	Validated
	ID *models.RecordID
}

func (f *validatedLink) MarshalCBOR() ([]byte, error) {
	if f == nil {
		return nil, nil
	}
	return cbor.Marshal(f.ID)
}

func (f *validatedLink) UnmarshalCBOR(data []byte) error {
	if err := cbor.Unmarshal(data, &f.ID); err == nil {
		// The link was not fetched, so only its record id is known.
		if f.ID != nil {
			idStr, err := cbor.RecordIDToString(f.ID.ID)
			if err != nil {
				return err
			}
			f.Validated.Node = som.NewNode[som.ULID](som.ULID(idStr))
		}
		internal.SetMarker(&f.Validated.Node, internal.MarkerLoaded|internal.MarkerPartial)
		return nil
	}
	type alias validatedLink
	var link alias
	err := cbor.Unmarshal(data, &link)
	if err == nil {
		*f = validatedLink(link)
	}
	return err
}

func fromValidatedLink(link *validatedLink) model.Validated {
	if link == nil {
		return model.Validated{}
	}
	res := Validated(link.Validated)
	return ToValidated(res)
}

func fromValidatedLinkPtr(link *validatedLink) *model.Validated {
	if link == nil {
		return nil
	}
	res := Validated(link.Validated)
	out := ToValidated(res)
	return &out
}

func toValidatedLink(node model.Validated) *validatedLink {
	if node.ID() == "" {
		return nil
	}
	rid := models.NewRecordID("validated", node.ID())
	link := validatedLink{Validated: FromValidated(node), ID: &rid}
	return &link
}

func toValidatedLinkPtr(node *model.Validated) *validatedLink {
	if node == nil {
		return nil
	}
	if node.ID() == "" {
		return nil
	}
	rid := models.NewRecordID("validated", node.ID())
	link := validatedLink{Validated: FromValidated(*node), ID: &rid}
	return &link
}

// The relations of Validated, as bits of its load state.
const (
	validatedFetchedLink internal.Relations = 1 << iota
)

// validatedFetchedBits reports which relations of the decoded record hold no
// unresolved links. A relation without any link counts as resolved.
func validatedFetchedBits(c *Validated) internal.Relations {
	var relations internal.Relations

	if c.Link == nil || !c.Link.IsPartial() {
		relations |= validatedFetchedLink
	}

	return relations
}

// ValidatedResolved reports whether the given relation path of the model was
// loaded from the database. The path is followed segment by segment, so a
// nested path is only resolved if every relation along it was fetched.
func ValidatedResolved(m *model.Validated, path string) bool {
	head, rest, _ := strings.Cut(path, ".")
	switch head {
	case "link":
		if !internal.Fetched(m).Has(validatedFetchedLink) {
			return false
		}
		if rest == "" {
			return true
		}
		if m.Link == nil {
			return true
		}
		return ValidatedResolved(m.Link, rest)
	}

	// An unknown relation is never resolved, so it is always fetched again.
	return false
}
