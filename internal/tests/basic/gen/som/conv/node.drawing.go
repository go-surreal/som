// Code generated by github.com/go-surreal/som, DO NOT EDIT.
package conv

import (
	models "github.com/surrealdb/surrealdb.go/pkg/models"
	som "som.test/gen/som"
	internal "som.test/gen/som/internal"
	cbor "som.test/gen/som/internal/cbor"
	types "som.test/gen/som/internal/types"
	model "som.test/model"
)

type Drawing struct {
	model.Drawing
}

func (c *Drawing) MarshalCBOR() ([]byte, error) {
	if c == nil {
		return cbor.Marshal(nil)
	}
	return cbor.Marshal(c.fields())
}

func (c *Drawing) fields() map[string]any {
	data := make(map[string]any, 7)

	// Embedded som.Node/Edge ID field
	if c.ID() != "" {
		data["id"] = models.NewRecordID("drawing", c.ID())
	}

	if !c.CreatedAt().IsZero() {
		data["created_at"] = &types.DateTime{Time: c.CreatedAt()}
	}
	if !c.UpdatedAt().IsZero() {
		data["updated_at"] = &types.DateTime{Time: c.UpdatedAt()}
	}
	data["name"] = c.Name
	if link := toShapeLink(c.Subject); link != nil {
		data["subject"] = link
	}
	if link := toColoredLink(c.Accent); link != nil {
		data["accent"] = link
	}
	if c.Annotations != nil {
		data["annotations"] = c.Annotations
	}

	return data
}

func (c *Drawing) UnmarshalCBOR(data []byte) error {
	var rawMap map[string]cbor.RawMessage
	if err := cbor.Unmarshal(data, &rawMap); err != nil {
		return err
	}

	// Embedded som.Node/Edge/View ID field
	if raw, ok := rawMap["id"]; ok {
		var recordID *models.RecordID
		if err := cbor.Unmarshal(raw, &recordID); err != nil {
			return err
		}
		var idStr string
		if recordID != nil {
			s, err := cbor.RecordIDToString(recordID.ID)
			if err != nil {
				return err
			}
			idStr = s
		}
		c.Node = som.NewNode[som.ULID](som.ULID(idStr))
	}

	if raw, ok := rawMap["created_at"]; ok {
		tm, _ := cbor.UnmarshalDateTime(raw)
		internal.SetCreatedAt(&c.Timestamps, tm)
	}
	if raw, ok := rawMap["updated_at"]; ok {
		tm, _ := cbor.UnmarshalDateTime(raw)
		internal.SetUpdatedAt(&c.Timestamps, tm)
	}
	if raw, ok := rawMap["name"]; ok {
		cbor.Unmarshal(raw, &c.Name)
	}
	if raw, ok := rawMap["subject"]; ok {
		var convVal *shapeLink
		cbor.Unmarshal(raw, &convVal)
		c.Subject = fromShapeLink(convVal)
	}
	if raw, ok := rawMap["accent"]; ok {
		var convVal *coloredLink
		cbor.Unmarshal(raw, &convVal)
		c.Accent = fromColoredLink(convVal)
	}
	if raw, ok := rawMap["annotations"]; ok {
		cbor.Unmarshal(raw, &c.Annotations)
	}

	// Mark the instance as fully loaded from the database
	internal.SetMarker(&c.Node, internal.MarkerLoaded)

	return nil
}

func FromDrawing(data model.Drawing) Drawing {
	return Drawing{Drawing: data}
}
func FromDrawingPtr(data *model.Drawing) *Drawing {
	if data == nil {
		return nil
	}
	return &Drawing{Drawing: *data}
}

func ToDrawing(data Drawing) model.Drawing {
	return data.Drawing
}
func ToDrawingPtr(data *Drawing) *model.Drawing {
	if data == nil {
		return nil
	}
	result := data.Drawing
	return &result
}

// DrawingFields returns the database keyed value map of a model. It is used by
// the query builder to derive pagination cursor values with correct database field
// names and types.
func DrawingFields(m *model.Drawing) map[string]any {
	c := Drawing{*m}
	return c.fields()
}

// drawingLink is a Drawing as referenced by another record. It marshals
// to its record ID only, but unmarshals from either a record ID or a fetched record.
type drawingLink struct {
	Drawing
	ID *models.RecordID
}

func (f *drawingLink) MarshalCBOR() ([]byte, error) {
	if f == nil {
		return nil, nil
	}
	return cbor.Marshal(f.ID)
}

func (f *drawingLink) UnmarshalCBOR(data []byte) error {
	if err := cbor.Unmarshal(data, &f.ID); err == nil {
		// The link was not fetched, so only its record id is known.
		if f.ID != nil {
			idStr, err := cbor.RecordIDToString(f.ID.ID)
			if err != nil {
				return err
			}
			f.Drawing.Node = som.NewNode[som.ULID](som.ULID(idStr))
		}
		internal.SetMarker(&f.Drawing.Node, internal.MarkerLoaded|internal.MarkerPartial)
		return nil
	}
	type alias drawingLink
	var link alias
	err := cbor.Unmarshal(data, &link)
	if err == nil {
		*f = drawingLink(link)
	}
	return err
}

func fromDrawingLink(link *drawingLink) model.Drawing {
	if link == nil {
		return model.Drawing{}
	}
	res := Drawing(link.Drawing)
	return ToDrawing(res)
}

func fromDrawingLinkPtr(link *drawingLink) *model.Drawing {
	if link == nil {
		return nil
	}
	res := Drawing(link.Drawing)
	out := ToDrawing(res)
	return &out
}

func toDrawingLink(node model.Drawing) *drawingLink {
	if node.ID() == "" {
		return nil
	}
	rid := models.NewRecordID("drawing", node.ID())
	link := drawingLink{Drawing: FromDrawing(node), ID: &rid}
	return &link
}

func toDrawingLinkPtr(node *model.Drawing) *drawingLink {
	if node == nil {
		return nil
	}
	if node.ID() == "" {
		return nil
	}
	rid := models.NewRecordID("drawing", node.ID())
	link := drawingLink{Drawing: FromDrawing(*node), ID: &rid}
	return &link
}

// DrawingResolved reports whether the given relation path of the model was
// loaded from the database. The model has no relations, so no path is.
//
// The relation Subject, Accent is not tracked: its field nests links deeper than a
// slice, so it is reported as not resolved and loaded again on every call.
func DrawingResolved(_ *model.Drawing, _ string) bool {
	return false
}
