// Code generated by github.com/go-surreal/som, DO NOT EDIT.

package internal

import (
	"errors"
	"strconv"
)

// ErrValidation is returned when a Validate method rejected a value. It is a
// narrower form of ErrInvalid, which matches such a rejection as well.
var ErrValidation = errors.New("validation failed")

// ValidationError reports that a Validate method rejected a value on its way
// into the database. Path locates the offending value within the model, e.g.
// "Address.Street" or "Tags[2].Name". It is empty if the model type itself
// rejected the write.
//
// Validation only ever runs on write, so a record that predates a rule can
// still be read back and fixed.
type ValidationError struct {
	// Path is the dot separated path of the value within the model, with
	// slice indexes in brackets. It is empty for the model itself.
	Path string

	// Err is the error the Validate method returned.
	Err error
}

func (e *ValidationError) Error() string {
	if e.Path == "" {
		return "validation failed: " + e.Err.Error()
	}

	return "validation failed for " + e.Path + ": " + e.Err.Error()
}

func (e *ValidationError) Is(target error) bool {
	return target == ErrValidation || target == ErrInvalid
}

func (e *ValidationError) Unwrap() error {
	return e.Err
}

// NewValidationError wraps the error a Validate method returned, so that the
// caller can get at the path of the offending value via errors.As.
func NewValidationError(path string, err error) error {
	return &ValidationError{Path: path, Err: err}
}

// FieldPath appends a field name to the path of the value holding it.
func FieldPath(prefix, name string) string {
	if prefix == "" {
		return name
	}

	return prefix + "." + name
}

// IndexPath appends the index of a slice element to the path of the slice.
func IndexPath(prefix string, index int) string {
	return prefix + "[" + strconv.Itoa(index) + "]"
}
