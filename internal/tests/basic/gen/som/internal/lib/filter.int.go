// Code generated by github.com/go-surreal/som, DO NOT EDIT.

package lib

// int_ also admits the pointer widths, so that an optional field can still be
// compared against a pointer as it always could.
type int_ interface {
	~int | ~int8 | ~int16 | ~int32 | ~int64 |
		~uint | ~uint8 | ~uint16 | ~uint32 | ~uint64 | ~uintptr |
		~*int | ~*int8 | ~*int16 | ~*int32 | ~*int64 |
		~*uint | ~*uint8 | ~*uint16 | ~*uint32 | ~*uint64 | ~*uintptr
}

type AnyInt[M any] interface {
	field[M]
	anyInt()
}

// Int is a filter for an integer field. It is deliberately not generic over
// the Go width of that field: SurrealDB stores every integer as one numeric
// type, so the width never reached the query. It only typed the argument of
// Equal and its siblings, and the conversions between widths handed on the
// very same key unchanged, making them no-ops in the rendered SurrealQL.
//
// Carried as a type parameter, the width instantiated Int -- and with it
// Numeric, Base and Comparable -- once per width per model, eighteen times
// over: the conversions returned each other's types, so reaching any one of
// them pulled in the whole set, for every model, whether or not it declared a
// single integer field.
//
// The comparisons are generic methods instead. A field still only accepts
// integers, and an int8 column can still be compared against an int8 without
// a conversion in between.
type Int[M any] struct {
	*Numeric[M, int64]
}

func NewInt[M any](key Key[M]) *Int[M] {
	return &Int[M]{
		Numeric: NewNumeric[M, int64](key),
	}
}

type IntPtr[M any] struct {
	*Int[M]
	*Nillable[M]
}

func NewIntPtr[M any](key Key[M]) *IntPtr[M] {
	return &IntPtr[M]{
		Int:      NewInt[M](key),
		Nillable: NewNillable(key),
	}
}

func (i *Int[M]) anyInt() {}

func (i *Int[M]) key() Key[M] {
	return i.Numeric.key()
}

func (i *Int[M]) Equal[V int_](val V) Filter[M] {
	return i.key().op(OpEqual, val)
}

func (i *Int[M]) NotEqual[V int_](val V) Filter[M] {
	return i.Numeric.Base.notSet(OpNotEqual, val)
}

func (i *Int[M]) In[V int_](vals []V) Filter[M] {
	return i.key().op(OpIn, vals)
}

func (i *Int[M]) NotIn[V int_](vals []V) Filter[M] {
	return i.Numeric.Base.notSet(OpNotIn, vals)
}

func (i *Int[M]) LessThan[V int_](val V) Filter[M] {
	return i.key().op(OpLessThan, val)
}

func (i *Int[M]) LessThanEqual[V int_](val V) Filter[M] {
	return i.key().op(OpLessThanEqual, val)
}

func (i *Int[M]) GreaterThan[V int_](val V) Filter[M] {
	return i.key().op(OpGreaterThan, val)
}

func (i *Int[M]) GreaterThanEqual[V int_](val V) Filter[M] {
	return i.key().op(OpGreaterThanEqual, val)
}

func (i *Int[M]) Float32() *Float[M, float32] {
	return NewFloat[M, float32](i.Base.prefix(CastFloat))
}

func (i *Int[M]) Float64() *Float[M, float64] {
	return NewFloat[M, float64](i.Base.prefix(CastFloat))
}
