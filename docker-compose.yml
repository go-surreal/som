version: "3.8"

services:

  surrealdb:
    image: surrealdb/surrealdb:nightly
    command: start --log trace --user root --pass root file:data/surreal
    ports:
      - "8020:8000"
    volumes:
      - type: bind
        source: ~/.local/share/docker/volumes/som/surrealdb
        target: /data
    restart: always
