version: "3.8"

services:

  surrealdb:
    image: surrealdb/surrealdb:v2.0.0-alpha.1
    command: start --user root --pass root --strict --allow-funcs --log trace file:data/surreal
    ports:
      - "8020:8000"
    volumes:
      - type: bind
        source: ~/.local/share/docker/volumes/som/surrealdb
        target: /data
    restart: always
