# Docker Compose

services:

  surrealdb:
    image: surrealdb/surrealdb:v2.0.0-beta.1
    command: start --user root --pass root --strict --allow-funcs --log trace surrealkv:data/surreal
    ports:
      - "8020:8000"
    volumes:
      - type: bind
        source: ~/.local/share/docker/volumes/som/surrealdb
        target: /data
    restart: always
